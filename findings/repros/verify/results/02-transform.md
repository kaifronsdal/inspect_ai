# Batch 02-transform — browser-verification results

**Port:** 7577 · **Logs:** `findings/repros/logs/02-transform/` · **Checks:** `findings/repros/verify/checks/F0{2,3}_*.py`
**Run:** `uv run --with playwright python findings/repros/verify/verify_one.py --batch 02-transform`

| ID | Verdict | Evidence (page text) | Notes |
|---|---|---|---|
| **F02.1** | **CONFIRMED** | After switching event-filter `Default→Debug`:<br>`SANDBOX: EXEC` / `SANDBOX: EXEC` / `SANDBOX: READ_FILE` render as **three individual sibling panels** alongside `MODEL CALL` — no `Sandbox Events` group panel anywhere. | `groupSandboxEvents` injects a synthetic span with `parent_id:null` and leaves each sandbox event's `span_id` pointing at the solver; `treeifyWithSpans` parents the events under the solver, the wrapper ends up empty, `filterEmpty` strips it. **Gotcha:** the inspect viewer's default event filter (`kDefaultExcludeEvents`, `sampleSlice.ts:18`) hides `sandbox` events entirely — the check has to click the *Events: Default → Debug* preset before any sandbox panel appears at all. |
| **F02.2 / F02.3** | **CONFIRMED** | Outline tree (`data-depth`):<br>`d0: scorer_ONE_…`<br>`d1:   scoring`<br>`d0: scorer_TWO_…`<br>`d1:   scoring`<br>`d0: error` | Both scorer spans land at depth 0 with **no** `scorers` wrapper above them. F02.2: synthetic wrapper keyed by `id=kBeginScorerId`, children re-parented to `kScorersSpanId` → lookup miss → wrapper empty → stripped. F02.3: scorer TWO at depth 0 confirms it was never collected (`!hasCollectedScorers` guard). |
| **F02.4** | **CONFIRMED** *(re-audited 2026-04-24)* | Outline `data-depth`: `d0/x=21.7 nested_under_main` · `d1/x=31.3 CHILD` · `d3/x=50.5 GRANDCHILD` · `d4/x=60.1 1 turn`. Transcript x-offsets: `CHILD=475.2` · `INFO=507.2` · `GRANDCHILD=507.2` · `INFO₂=523.2`. | Depth jumps **1 → 3**; CHILD→child step is **2×** the unit (19.2 px outline / 32 px transcript vs 9.6 / 16 elsewhere). `unwrapNode` set `CHILD.depth = main.depth` but did not recurse. **CORRECTED on re-audit:** the repro's `observed`/`expected` text wrongly says to compare GRANDCHILD against its *sibling INFO* — those two are **both** at depth 3 (equally wrong, so flush with each other) and that comparison cannot distinguish bug from no-bug. The correct comparison is **CHILD vs its children** (1 vs 3). See [`REAUDIT.md`](../REAUDIT.md#part-1--f024-root-cause). Screenshots: `artifacts/reaudit/F02.4-{outline,transcript}-zoom.png`. |
| **F02.5** | **CONFIRMED** | Transcript x-offsets:<br>`x=472.0  TOOL: TRANSFER_TO_SUBAGENT`<br>`x=472.0  AGENT: AGENT_SPAN_SHOULD_BE_INDENTED_UNDER_TOOL_…`<br>`x=483.2  INFO: REPRO`<br>`x=483.2  STEP: INNER_WORK_…` | The agent span (child of the ToolEvent) renders at the **same** indent as its parent. `skipThisNode` → `reduceDepth(children, 2)` over-reduces the first level. *(Secondary claim "grandchildren jump two levels" not observed — agent.children are at +1, not +2, because `discard_solvers_span` later applies another uniform −1; the primary same-indent bug is unambiguous.)* |
| **F03.2** | **FALSE_POSITIVE** | `checkbox[1] before=True → after-input-click=False → after-label-click=True` | Clicking the `<input>` directly toggles the option **once**, not zero times. The finding's premise is right (both `onChange` and the bubbled wrapper `onClick` fire — `stopPropagation()` on a `change` event does not block the native `click`), but its conclusion is wrong: `toggleMarkerKind` (`useTimelineConfig.ts:119-128`) reads `storedKinds` from the **render closure** and calls `setStoredKinds(next)` (non-functional). Two synchronous calls compute the *same* `next` from the *same* stale closure → both set the identical value → single toggle. Same applies to `setIncludeUtility(!config.includeUtility)` etc. The "flicker and revert" behaviour does **not** occur. |
| **F03.3** | **NOT_REPRODUCED** | Swimlane rows: `['main', 'my_pkg/my_module/weird_agent', 'my_pkg/my_module/grandchild']`<br>Breadcrumb after selecting grandchild: `['main', 'my_pkg/my_module/weird_agent', 'my_pkg/my_module/grandchild']` | Breadcrumb is exactly the real ancestor chain — **no phantom segments, no gaps**. `buildBreadcrumbs` does split the key on `/` and looks up phantom prefixes (`main/my_pkg`, `main/my_pkg/my_module`, …) but the `if (layout)` guard at `TimelineSwimLanes.tsx:63` silently drops misses, and real ancestors are *always* hit (child key = `${parentKey}/${name}` so parent key is always a prefix). Same guard pattern in `visibleLayouts` (`:234-246`) and `parentKeys` (`:187-199`). The code structure matches the finding, but the claimed observable ("breadcrumbs show gaps") **does not occur** — borders on FALSE_POSITIVE for the impact claim. A breaking case would require a *colliding* sibling row whose key equals a phantom prefix; the repro doesn't construct one. *(Bonus: F03.23 IS visible here — the **outline** labels these spans `my_module`, dropping `my_pkg/` and `/weird_agent` via `parsePackageName(...).module`.)* |
| **F03.4** | **CONFIRMED — partial** *(re-audited 2026-04-24, repro regenerated)* | Chevron `<i>` click (force, 9×10 px target): `selected SECOND False→True`; `scrollTop 2100→2100`; `url_changed=False`; `child_a` collapsed | **Repro regenerated** with plain `span(name)` (was `type='agent'` → no chevron). The chevron `onClick` (`OutlineRow.tsx:66-70`) lacks `e.stopPropagation()` and **does** bubble to the row's `onClick` → `onSelect` fires → selection highlight jumps. **However**, the finding's primary impact claim — *"scrolls the main transcript"* — is **FALSE** in `apps/inspect`: `TranscriptPanel.tsx:408-418` does **not** set `outline.onNavigateToEvent` (only `apps/scout/.../TimelineEventsView.tsx:228` does), and the chevron sits outside the `<Link>`, so URL/`?event=` is unchanged → no scroll. Manual user test confirms: chevron click does NOT scroll. **Recommend downgrade MEDIUM→LOW**: selection-highlight jump only in inspect; scroll-jump is scout-only. Screenshot: `artifacts/reaudit2/F03.4-after-chevron.png`. |
| **F03.5** | **CONFIRMED** *(re-audited 2026-04-24, repro regenerated)* | Nesting OK (level1@x=275 … level4@x=323, 16 px steps). After collapsing `LEVEL1` in transcript: `l4_hidden=True`. Click `level4_CLICK_ME` `<a>` in outline → `url_changed=True` (`?event=DQpk…`), `scrollTop 0→0`, `l4_visible=False` | **Repro regenerated** with plain `span(name)` + 20 InfoEvent padding (was `type='agent'` → flattened to swimlane siblings, `level1` removed by `unwrap_agent_solver`). Clicking the outline link **does** update the URL to `?event=<level4-uuid>`, but `TranscriptVirtualListComponent.tsx:82-93` resolves `initialEventId` against `flattenedNodes` (built honouring `collapsedTranscript`) → `findIndex → -1` → `initialEventIndex=undefined` → **silent no-op**: no auto-expand, no scroll, no feedback. Screenshots: `artifacts/reaudit2/F03.5-after-{collapse,outline-click}.png`. |

## Summary

| Verdict | Count | IDs |
|---|---|---|
| CONFIRMED | 6 | F02.1, F02.2/F02.3, F02.4, F02.5, F03.4², F03.5 |
| FALSE_POSITIVE | 1 | F03.2 |
| NOT_REPRODUCED | 1 | F03.3 |
| INCONCLUSIVE | 0 | — |

² Partial — bubbling/selection-jump confirmed; the finding's "scrolls the main transcript" impact claim is false for `apps/inspect` (scout-only). Downgrade MEDIUM→LOW.

## Repro fixes applied (2026-04-24)

- **F02.4**: `observed`/`expected`/`where_to_look` rewritten to compare REFERENCE vs BUGGY subtrees side-by-side (was comparing two equally-mis-depthed siblings). See [`REAUDIT.md`](../REAUDIT.md) Part 1.
- **F03.4 / F03.5**: regenerated using `async with span("name")` (no `type=`). The previous `type="agent"` spans triggered swimlane/agent-card mode (no chevrons, flat siblings) so neither bug was reachable. Both `.eval` files replaced; check scripts rewritten to use the new logs directly (no F02.4 fallback). F03.4's check now also asserts `scrollTop` and URL are **unchanged**, documenting that the finding's scroll-impact claim is inspect-false.
- **F02.1**: no fix needed, but reviewers should know the check must flip the event filter to *Debug* first (`kDefaultExcludeEvents` hides `sandbox`).
- **F03.3**: to actually surface a wrong breadcrumb, the repro would need a sibling agent whose name collides with a phantom prefix (e.g. a sibling of `weird_agent` named `my_pkg`). Without that, all `/`-split lookups are guarded and harmless.

# F10.4 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel, expanded, no sub-agent wrap)
**Inaccuracies found:** 3

## Sup/footnote number claims — verified accurate

DOM extraction on Messages tab, turn 2:
- numeric `<sup>` inner_texts: `['1', '2', '1', '2']` ✓ matches "both blocks show ¹ ²"
- citation list inner_text: `1 CITE-A / 2 CITE-B / 3 CITE-C / 4 CITE-D` ✓ matches "footnote list runs 1–4"
- footnote entries are `<a href="https://example.invalid/...">` external links; inline sups are bare `<sup>N</sup>` with no anchor

## Inaccuracies fixed

1. **Title `<sup>` parsed as HTML** — old title `"Citation <sup> numbers don't match…"` rendered as "Citation" followed by the rest of the line in tiny superscript (the literal `<sup>` tag was interpreted by the markdown renderer, breaking the bold title and the INPUT header). → `"Citation superscript numbers don't match the footnote list"`.
2. **"Clicking footnote #3 (CITE-C) maps to no inline ³"** — footnote entries are external-URL `<a>` links (open `example.invalid/cite-c` in a new tab); there is no click-through to inline markers in either direction. → `"Footnotes 3 and 4 (CITE-C, CITE-D) have no matching inline ³ or ⁴"` and added to `extra`: `"The inline sups are plain numbers with no anchor/link — the number is the only way to correlate text with source."`
3. **Vague navigation target** — `"the assistant turn after this description"` → `"the assistant message (turn 2)"` so it matches the visible row number in the Messages tab.

Also tightened `observed` to state the exact rendered sequence: `"the four sups read ¹ ² ¹ ²"`.

## Before/after screenshots
- before: artifacts/accuracy/F10.4-before.png (transcript), artifacts/accuracy/F10.4-before-messages.png
- after: artifacts/accuracy/F10.4-after.png

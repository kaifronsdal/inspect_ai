# F20.5 — Description Accuracy

**Verdict (unchanged):** CONFIRMED_MINOR
**Banner visible at Default:** no → fixed
**Inaccuracies found:** 4 (+ 1 structural fix)

## Inaccuracies fixed

1. **Banner hidden inside collapsed `SUB-AGENT: BANNER`** (chain() problem) → replaced `solver=chain(banner(), generate())` with a single `repro_solver()` that calls `emit_bug_banner(DESC)` inline then `await generate(state)`. Banner is now the first expanded panel under the Default filter.
2. **observed:** "the sentinel string … appears **nowhere** on this tab" → "There is no **Target** heading or row anywhere in the tab body — the view never reads `sample.target`." — The sentinel *did* appear on the tab (twice: in the Input prompt and in the scorer Explanation); the accurate claim is that no **Target** heading/row is rendered.
3. **observed:** "(Ctrl-F … it's only in the header row above the tabs and in this prompt text.)" → removed. — Ctrl-F also matched the scorer Explanation, so the parenthetical was wrong. Also removed the sentinel from the scorer explanation (now `"Trivial scorer — always returns 1.0."`) to keep the demo clean.
4. **expected:** "…without flipping back to the header or Transcript" → "A **Target** heading + value rendered alongside **Input**, for parity." — The header is pinned above the tabs and on screen simultaneously; "flipping back" misrepresents the UI.
5. **Missing minor-impact note** → added `extra=`: "**Note:** impact is minor — the pinned sample header directly above the tab strip already shows a labelled **Target** column with the full sentinel, visible on screen at the same time as the Scoring tab (no navigation needed). The only residual gap is that the header truncates long values while **Input** gets a full expandable panel."

## Re-verified (after regeneration)

- Transcript (Default filter): panel[0] = `INFO: BUG-REPRO` (expanded), no `SUB-AGENT: BANNER` anywhere.
- Scoring tab labels: `['INPUT', 'SCORER', 'ANSWER', 'SCORE', 'EXPLANATION']` — no `TARGET` ✓ matches `observed`.
- Pinned header: labelled `TARGET` column shows full `F20.5_TARGET_SHOULD_APPEAR_IN_SCORING_TAB` ✓ matches `extra` note.
- New log: `2026-04-24T22-05-53-00-00_F20.5-scoring-tab-omits-target_8fvt9CJtHgBCPAxS6QVrfH.eval`

## Before/after screenshots

- before: artifacts/accuracy/F20.5-before.png (transcript — banner hidden in SUB-AGENT)
- before: artifacts/accuracy/F20.5-before-scoring.png
- after: artifacts/accuracy/F20.5-after.png (transcript — banner expanded at top)
- after: artifacts/accuracy/F20.5-after-scoring.png

# F30.2 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel, expanded, inside open `SOLVER: RAISE_FOR_ERROR_STATUS` span)
**Inaccuracies found:** 2

## Inaccuracies fixed

1. **`where_to_look`**: "(top right, next to the task name)" → "(top right, next to the 'Task Failed' label)" — the task name is on the far **left** of the title bar; the status icon sits on the far **right** beside the `Task Failed / (1 sample)` label (StatusPanel bbox x≈1258 in a 1400-wide navbar). The old text would send the user to the wrong end of the bar.
2. **`observed`**: "List shows `bi-exclamation-circle-fill` … header shows `bi-x-circle`" → added visual descriptions: "**filled red circle with a white '!'** (`bi-exclamation-circle-fill`)" and "**outlined circle with an X** (`bi-x-circle`)" — class names alone are invisible to a human looking at the screen; the glyph descriptions let them confirm the bug without DevTools. (Class names retained and re-verified against DOM: list `<i class="bi bi-exclamation-circle-fill">`, header `<i class="bi bi-x-circle">`.)

## Verified accurate (no change)

- Log-list **Status** column exists (`col-id="status"`, header text `Status`).
- Clicking the row navigates to log detail (URL → `#/logs/<file>.eval`).
- Header status panel renders `bi-x-circle` + text `TASK FAILED / (1 SAMPLE)`.
- `expected` and `extra` claims about `ApplicationIcons` vs `ApplicationIcons.logging` mapping match source (`icons.ts`).

## Before/after screenshots

- before: artifacts/accuracy/F30.2-before.png (transcript), artifacts/accuracy/F30.2-before-list.png (grid), artifacts/accuracy/F30.2-before-header.png (title bar)
- after: artifacts/accuracy/F30.2-after.png

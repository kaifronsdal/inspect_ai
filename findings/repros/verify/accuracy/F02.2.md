# F02.2 — Description Accuracy

**Verdict (unchanged):** CONFIRMED_MINOR (legacy-log-only path)
**Banner visible at Default:** yes (expanded `INFO: BUG-REPRO` inside the open solver span, first thing visible)
**Inaccuracies found:** 2

## Inaccuracies fixed

1. **where_to_look** — "Also open the **Outline** (left panel)" → "also see the outline tree on the left."
   The outline tree is visible by default (its only control is a *Hide transcript outline* close button) and carries no visible "Outline" label, so there is nothing to "open". Reworded as a passive look-here instruction.

2. **extra** — added leading minor-impact note (required for CONFIRMED_MINOR):
   `**Note:** impact is minor — only affects pre-May-2025 logs. Since 2025-05-23 inspect emits a real type="scorers" span and injectScorersSpan short-circuits; this fixup path is dead for modern logs.`

Also tightened **observed** with a concrete anchor that matches the screen: "(depth 0 in the outline, same indent as the solver)" — both scorer rows are `data-depth="0"` and both `SCORER:` panels sit at x=415 alongside `SOLVER:` and `ERROR`.

## Claims verified accurate (unchanged)

- "Transcript tab" — tab exists with that exact name.
- "scorer_ONE_… and scorer_TWO_… render as top-level siblings" — outline depth 0, panel x=415 (root indent). ✅
- "No `Scorers` group node" — no such panel or outline row exists. ✅
- "intentional error" note — `ERROR` panel present at root as described. ✅
- F02.2/F02.3 mechanics in `extra` — match `treeify.ts` source per `per-finding/F02.2.md`. ✅

## Before/after screenshots

- before: artifacts/accuracy/F02.2-before.png
- after: artifacts/accuracy/F02.2-after.png

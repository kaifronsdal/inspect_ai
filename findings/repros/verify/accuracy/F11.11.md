# F11.11 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** no → **fixed** (was hidden in collapsed `SUB-AGENT: BANNER`)
**Inaccuracies found:** 4

## Inaccuracies fixed

1. **chain() banner-hiding** — `solver=chain(banner(), use_tools(...), generate())` wrapped every step in a collapsed `SUB-AGENT:` span, so the Default-filter transcript showed only two grey rows and the `INFO: BUG-REPRO` panel was invisible. → Replaced with a single `repro_solver()` that calls `emit_bug_banner(DESC)`, sets `state.tools`, and awaits `generate(state)`. Banner now renders as the first expanded panel.

2. **`where_to_look`**: "expand the **Tool** event for `write_file`, then the nested **Approval** event" → "the thin row with a ✏️ pencil icon and the label **MODIFIED**, directly below the **TOOL: WRITE_FILE** panel (and above the second **MODEL CALL** panel). It is a single non-expandable line — there is no separate 'Approval' panel." — The approval row is **not** nested inside the Tool panel (it's a sibling), is **not** labelled "Approval", and is **not** expandable. DOM confirms it's a standalone `.card` with no `event-panel-*` ancestor.

3. **`observed`**: "Only `decision: modify` and the explanation are shown" → "The row renders only the pencil icon, the decision label **Modified**, and the `explanation` string." — Actual rendered chrome is `<pencil icon> + "Modified" + <explanation>`; the literal text `decision: modify` never appears.

4. **`extra`**: "Ctrl-F the rendered Approval card for any of the three sentinels above — none will match" → "Switch to the sample **JSON** tab and search for `\"event\": \"approval\"` — `approver`, `modified`, and `message` are all populated in the log but never read by `ApprovalEventView.tsx`." — The old claim was **false**: the explanation string itself embedded two of the sentinels, so Ctrl-F *did* match them in the row. Also rewrote the `explanation` to drop the embedded sentinels so the row is now a clean negative for them (verified: `F11.11_APPROVER_NAME` / `F11.11_MODIFIED_ARG` no longer appear in the row text).

## Re-verification (after)

Default filter, no clicks:
- Panels: `SOLVER: REPRO_SOLVER` (open) → `INFO: BUG-REPRO` (expanded, full markdown table visible) → `MODEL CALL` → `TOOL: WRITE_FILE` → ✏️ `MODIFIED` row → `MODEL CALL`
- Approval row text = `"MODIFIED\nRewrote \`path\` to a sandboxed value. (This sentence is \`ApprovalEvent.explanation\` …)"` — icon + label + explanation only, exactly as `observed` now states
- JSON tab: `"event": "approval"` present with `approver`, `modified`, `message` all populated (sentinels found)

## Before/after screenshots

- before: `artifacts/accuracy/F11.11-before.png`
- after: `artifacts/accuracy/F11.11-after.png`

# F03.5 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (`INFO: BUG-REPRO` panel, expanded, second from top under `SOLVER: DEEP_NESTING`)
**Inaccuracies found:** 0

## Audit

Every claim in the description was checked against the live viewer and source:

| Claim | Location | Verified |
|---|---|---|
| "Transcript tab" exists | sample tabs | ✅ |
| "transcript (right panel)" | layout | ✅ right of outline |
| "click the chevron on `LEVEL1_COLLAPSE_ME`" | transcript panel | ✅ `bi-chevron-down` present on header `LEVEL1_COLLAPSE_ME: LEVEL1_COLLAPSE_ME`; click → 31 panels collapse to 3 |
| "outline (left panel)" | layout | ✅ left tree |
| "click `level4_CLICK_ME_in_outline`" | outline | ✅ exact link text present; click changes URL to `…/transcript?event=<uuid>` |
| "transcript stays collapsed at `level1`, does not scroll, gives no feedback" | post-click | ✅ panel count stays 3; `level1` chevron stays `bi-chevron-right`; `F03.5 SCROLL TARGET` sentinel absent from DOM |
| `TranscriptVirtualListComponent` / `flattenedNodes` / `collapsedTranscript` / `findIndex → -1` / `initialEventIndex` | source | ✅ `TranscriptViewNodes.tsx:115-148`, `TranscriptVirtualListComponent.tsx:73-89` |
| `collapseState.outline` vs `collapseState.transcript` independent | source | ✅ `TranscriptLayout.tsx:793,849` |
| Expected: auto-expand `level1`/`level2`/`level3`, scroll to `level4_CLICK_ME_in_outline` | span names | ✅ all four span names exist in both outline and transcript |

## Inaccuracies fixed

None — description is accurate as written. No task edit, no regeneration.

## Before/after screenshots

- before: `artifacts/accuracy/F03.5-before.png`
- after: `artifacts/accuracy/F03.5-after.png` (identical — no changes)
- stepped (post-collapse + post-outline-click, demonstrating the bug): `artifacts/accuracy/F03.5-before-stepped.png`

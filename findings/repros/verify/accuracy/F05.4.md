# F05.4 — Description Accuracy

**Verdict (unchanged):** CONFIRMED_MINOR
**Banner visible at Default:** yes (INFO: BUG-REPRO panel, expanded, top of transcript — no chain() fix needed)
**Inaccuracies found:** 2

## Inaccuracies fixed

1. "the 1-tool Summary tab body is itself **near-empty**" → "**empty** (no tool chips) … don't expect content in it" — DOM shows the Summary body is completely blank (`inner_text` = title + tab labels only). Tightened wording and made explicit that no tool chips render, so a reader doesn't go looking for them (per F05.1 interaction).
2. *(missing)* → added "**Note:** impact is minor — state events are hidden behind the Debug filter; the Summary body the 3-tool case is missing would be blank anyway until F05.1 is fixed; and the common `use_tools()` path (which also replaces `tool_choice`) hits the separate `use_tools` signature where the preview *does* fire. Cosmetic, one-character fix." — required for CONFIRMED_MINOR verdict; reasons lifted from per-finding analysis.

## Claims verified accurate (no change)

- `where_to_look`: "Events" filter button reads `Events: Default`; `Debug` option exists; switching reveals exactly **2** `STATE UPDATED` panels; both expandable via chevron. ✅
- `observed`: CONTROL panel sub-tabs = `["SUMMARY", "DIFF"]`; BUG panel sub-tabs = `[]` with raw jsondiffpatch body (`0: {beta_tool…}, 1: {gamma_tool…}, 2: {delta_tool…}`). CONTROL/BUG labels match the `▼ CONTROL (1 tool)` / `▼ BUG (3 tools)` INFO markers above each panel. ✅
- `observed` does **not** claim the 1-tool Summary shows tool chips — it only claims the Summary/Diff *pills* appear. ✅
- `expected`: describes intended design (tool-chip grid under a Tools label) — correctly framed as expected-after-fix, not current behaviour. ✅

## Before/after screenshots

- before: artifacts/accuracy/F05.4-before.png
- after: artifacts/accuracy/F05.4-after.png

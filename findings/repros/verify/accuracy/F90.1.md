# F90.1 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (already inside `repro_solver`, no chain() fix needed)
**Inaccuracies found:** 5

## Inaccuracies fixed

1. **where_to_look (A):** "the timestamp in the sample-detail header / Metadata-tab Time card (sv-SE: `2026-04-22 15:45:12`)" → "hover any event-panel title — tooltip reads e.g. `04/24/26, 8:47:45 PM`; or Model Call → **ALL** subtab → **CLOCK TIME** → START/END". The sample-detail header shows no datetime at all, and the Metadata→TIME card renders **durations** (`Working: 0.3 sec / Total: 0.3 sec`), not datetimes. This was the headline inaccuracy flagged in per-finding/F90.1.md.
2. **where_to_look (B):** demoted "Also compare the log-list Completed column" from afterthought to the primary sv-SE surface: "Click the back arrow to the **log list** and read this task's **Completed** column — e.g. `2026-04-24 20:47:46`". This is the only verifiable sv-SE surface in the repro.
3. **observed:** "Sample header / log list use `YYYY-MM-DD HH:mm:ss`" → "Log-list **Completed** column uses `YYYY-MM-DD HH:mm:ss`". Dropped the false "sample header" claim. Also softened "on one screen" → "one click apart" since log-list and transcript are separate views.
4. **expected:** "...so transcript event panels match the sample header" → "...match the log-list Completed column". Same wrong-surface fix.
5. **InfoEvent body** (the second `INFO: F90.1` panel): "Compare it to the sample header above which uses the app-local sv-SE formatter" → "Compare it to the **log-list Completed column** (back arrow → log list)". The "sample header above" doesn't show any sv-SE datetime.

Additional cleanup: replaced fabricated example timestamps (`2026-04-22 15:45:12` / `04/22/26, 3:45:12 PM` — never matched actual eval data) with realistic `e.g.` values; clarified `extra` to name the concrete import sites (`hooks.tsx:265` vs `*EventView` / `EventTimingPanel`); added a **Note** in `extra` flagging that the original finding's sample-header citation is wrong (those surfaces render durations via `formatTime`).

## Re-verification (after regen)

- Banner `INFO: BUG-REPRO` visible + expanded at panel #1 under Default filter.
- Hover tooltip on event-panel headers: `04/24/26, 10:20:49 PM` ✓ (locale 12-h).
- Model Call panel has subtabs `SUMMARY / ALL / API`; ALL → CLOCK TIME → START = `04/24/26, 10:20:49 PM` ✓.
- Log-list `completedAt` cells: `2026-04-24 22:21:39` etc. ✓ (sv-SE 24-h).
- Second InfoEvent body references log-list Completed column, not sample header ✓.

## Before/after screenshots
- before: artifacts/accuracy/F90.1-before.png
- before (Metadata tab — proves Time card shows durations): artifacts/accuracy/F90.1-before-metadata.png
- before (log-list — proves Completed column is sv-SE): artifacts/accuracy/F90.1-before-loglist.png
- after: artifacts/accuracy/F90.1-after.png

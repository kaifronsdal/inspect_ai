# F40.5 — Description Accuracy

**Verdict (unchanged):** CONFIRMED_MINOR
**Banner visible at Default:** no → **fixed** (was inside collapsed `SUB-AGENT: BANNER`)
**Inaccuracies found:** 4

## Inaccuracies fixed

1. **chain() banner-hiding** — `solver=chain(banner(), generate())` wrapped the InfoEvent in a collapsed `SUB-AGENT: BANNER` span, so the description was invisible at the Default filter. → Replaced with a single `repro_solver()` that calls `emit_bug_banner(DESC)` inline before `await generate(state)`. Banner now renders as a top-level expanded `INFO: BUG-REPRO` panel.

2. **where_to_look: missing Debug-filter step** — said "Transcript tab → expand the **Sample Init** event", but `sample_init` is excluded by the Default event filter so nothing is there to expand. → Added "set the **Events** filter (top-right) to **Debug**" as the first step.

3. **where_to_look: wrong panel names** — said "expand the **Sample Init** event → switch to its **Metadata** sub-tab". There is no panel titled "Sample Init"; the actual path is a span titled **Init** which, when expanded, reveals a child panel titled **Sample** that has the **Sample / Metadata** sub-tabs. → Rewrote as "expand the **Init** span → in the **Sample** panel that appears, click its **Metadata** sub-tab → look at the `web_search` row."

4. **extra: no minor-impact note** — verdict is CONFIRMED_MINOR but the description gave no severity context. → Added `**Note:** impact is minor — this web_search renderer is orphaned legacy code. Real web-search server-tool results are rendered by ServerToolCall.tsx and never reach RenderedContent; the only live path here is MetaDataGrid with a metadata key literally named web_search, which real evals don't produce.` Also folded the source-file pointers into this note.

## Re-verification

Followed the new `where_to_look` exactly: Events filter → Debug ✓, `INIT` span present ✓, expand → `SAMPLE` panel with sub-tabs `[SAMPLE, METADATA]` ✓, click Metadata → single `WEB_SEARCH` row whose value is `<span>{"query":"F40.5 should render...}</span>` ✓, zero `<a href*=example.com>` links ✓. `observed` matches screen.

## Before/after screenshots
- before: artifacts/accuracy/F40.5-before.png
- after: artifacts/accuracy/F40.5-after.png
- after (bug location): artifacts/accuracy/F40.5-after-metadata.png

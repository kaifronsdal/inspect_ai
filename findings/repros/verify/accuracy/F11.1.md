# F11.1 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** no → **fixed** (was hidden in collapsed `SUB-AGENT: BANNER`)
**Inaccuracies found:** 4

## Inaccuracies fixed

1. **chain() banner-hiding** — `solver=chain(banner(), use_tools(...), generate())` wrapped everything in collapsed `SUB-AGENT: BANNER` / `SUB-AGENT: GENERATE` spans, so neither the banner nor the tool panels were visible at the Default filter. → Replaced with a single `repro_solver()` that calls `emit_bug_banner(DESC)`, sets `state.tools`, and `await generate(state)` inline. After: no `SUB-AGENT` rows; `INFO: BUG-REPRO` is the first panel and both `Tool:` panels are top-level.
2. **"expand the two Tool events"** → **"compare the two `Tool:` panels … both already expanded"** — the tool event panels render expanded by default (output `<pre>` is visible without any click), so "expand" was a misleading instruction.
3. **Panel naming** — "two **Tool** events (`good_tool` then `bad_tool`)" → "`Tool: good_tool` vs `Tool: bad_tool`" — matches the on-screen headers `TOOL: GOOD_TOOL` / `TOOL: BAD_TOOL` (uppercased via CSS).
4. **Messages-tab pointer** — "the two assistant tool-call rows" → "rows 2 and 3 (assistant turns calling `good_tool` / `bad_tool`)" — gives the actual row numbers a reader will see.

Also tightened `observed` to name the panels explicitly ("The `Tool: bad_tool` panel renders … identical CSS classes, icon, and header colour to … `Tool: good_tool`") and added a note in `extra` that the ❌/✅ emoji are injected by the repro, not by the viewer — so a reader doesn't mistake them for viewer-added error styling.

## Re-verification (after regenerate)

- `SUB-AGENT` in body: **False**
- `INFO: BUG-REPRO` panel at top, expanded, table renders with new `where_to_look` text
- `TOOL: GOOD_TOOL` / `TOOL: BAD_TOOL` panels both visible at Default filter, both expanded
- Output `<pre>` className identical on both: `tool-output _outputPre_8cjqa_1` → bug still reproduces
- Messages tab rows 2 & 3 show `good_tool(q: "x")` / `bad_tool(q: "x")` with results

## Before/after screenshots

- before: `artifacts/accuracy/F11.1-before.png` (two collapsed SUB-AGENT rows, nothing else)
- after: `artifacts/accuracy/F11.1-after.png` (banner + tool panels visible at Default)
- after (messages): `artifacts/accuracy/F11.1-after-messages.png`

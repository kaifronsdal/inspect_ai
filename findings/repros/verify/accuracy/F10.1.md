# F10.1 — Description Accuracy

**Verdict (unchanged):** CONFIRMED (severity downgraded HIGH → MEDIUM)
**Banner visible at Default:** yes (INFO: BUG-REPRO expanded inside SOLVER span; no chain() problem)
**Inaccuracies found:** 3

## Inaccuracies fixed

1. **where_to_look:** "(default `collapseToolMessages` mode)" → removed. This is internal viewer-config jargon; no such mode/toggle is visible in the UI, so a reader has nothing to match it against. Replaced with concrete navigation: "Scroll past this description to rows 2 and 3."
2. **observed:** "jumps straight from the second user turn to the third user turn" → "Only **3 rows** render, all labelled USER. Row 2 (…) is immediately followed by row 3 (…)". Made the row-count claim explicit and verifiable. DOM-verified: numbered rows are exactly `[(1, USER), (2, USER), (3, USER)]`; sentinel tail `IF THIS LINE IS MISSING` is absent; `orphaned_tool` is absent.
3. **extra:** Added required severity note — "**Note:** impact is MEDIUM (downgraded from HIGH) — triggering this requires a malformed message sequence (a `tool` message not following an `assistant` tool-call); no standard `generate()` loop produces this." Also tightened the JSON cross-check to state the exact role sequence `[user, user, tool, user]` and `tool_call_id: orphan-001` so the reader knows what to look for, and added the `tool_calls` guard detail to the `ChatMessageRow` explanation (it requires `role=assistant` *and* `tool_calls`, not just the role).

## Row-count verification (Messages tab)

| Claim | On screen | Match |
|---|---|---|
| 3 rows total | `[('1','USER'),('2','USER'),('3','USER')]` | ✅ |
| All labelled USER | role labels: `['USER','USER','USER']` | ✅ |
| Nothing between rows 2 & 3 | sentinel/`orphaned_tool` absent from `#messages-contents` | ✅ |
| Raw log has 4 messages | JSON tab: roles `[user,user,tool,user]`, `orphan-001` present | ✅ |

## Before/after screenshots

- before: artifacts/accuracy/F10.1-before.png (transcript), artifacts/accuracy/F10.1-before-messages.png
- after: artifacts/accuracy/F10.1-after.png (transcript), artifacts/accuracy/F10.1-after-messages.png

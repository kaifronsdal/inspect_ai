# F21.10 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** no (hidden in collapsed `SUB-AGENT: BANNER`) → **fixed**
**Inaccuracies found:** 5

## Inaccuracies fixed

1. **Banner hidden by `chain()`** — both `@task` defs used `solver=chain(banner(), generate())`, which wrapped the `INFO: BUG-REPRO` panel inside a collapsed `SUB-AGENT: BANNER` span at the default filter. → Replaced with a single `repro_solver()` that calls `emit_bug_banner(DESC)` inline before `await generate(state)`; banner is now the first, expanded panel in the Transcript tab. (Also dropped the now-unused `chain` / `generate` imports.)
2. **`where_to_look` step 1 vague** — "open the cross-log **Samples** panel" → "click the **Samples** button (top-right, next to *Tasks* / *Folders*) to open the cross-log grid; scroll right to the `passfail` / `numeric` / `dictscore` columns". The control is a toolbar button, not a panel; verified clicking it routes to `#/samples/`.
3. **`where_to_look` step 2 incomplete** — "look at its per-log Samples tab" → added "click the **SCORERS:** dropdown (top-right) → **All** so the `PASSFAIL`, `NUMERIC`, `A`, `B` columns all appear". By default the single-log list only shows the `PASSFAIL` column, so the numeric/dict comparison was impossible to make without this missing step.
4. **`observed` dict-score wording wrong** — "single-log → key/value grid" → "split into separate per-key columns `A`=`1`, `B`=`0`". The single-log list does not render a key/value grid in one cell; it creates one column per dict key.
5. **`observed` ID-sort claim removed** — "Sample ID `10` sorts after `2` lexically in the multi-log grid (`String(id)`) but numerically in the single-log list" was **false**: both views show order `2, 10`, and clicking the multi-log `Sample ID` header does not reorder rows at all. Dropped the claim (it was also off-topic for a score-formatting finding). Adjusted the numeric example to id `2` (`2` vs `2.000`) so all three bullets reference the same row.

Also tightened `extra` ("≥2 logs … so the multi-log Samples panel is available" → the batch already has 9 logs; rephrased to explain the two F21.10 logs simply provide multiple rows with these scorers).

## Re-verification (after)

- Transcript, Default filter: panel[0] = `INFO: BUG-REPRO` with full markdown table; no `SUB-AGENT` spans.
- Log-listing → **Samples** button → `#/samples/`: `passfail`=`C` (plain text), `numeric`=`2.000`, `dictscore`=`{"a":1,"b":0}` on the `F21.10_multilog_*` rows.
- `F21.10_multilog_a` → Samples tab → SCORERS → **All**: headers `PASSFAIL | NUMERIC | A | B`; row id `2` = green-circle `C`, `2`, `1`, `0` (innerHTML contains `_circle_… _green_…`).
- Every navigation step in the new `where_to_look` works exactly as written; every `observed` bullet matches the DOM.

## Before/after screenshots

- before: `artifacts/accuracy/F21.10-before.png` (banner collapsed inside SUB-AGENT)
- after: `artifacts/accuracy/F21.10-after.png` (banner expanded at top)
- supporting: `artifacts/accuracy/F21.10-before-root.png`, `F21.10-before-grid.png`, `F21.10-before-singlelist.png`, `F21.10-before-scorer-dropdown.png`, `F21.10-after-grid.png`, `F21.10-after-singlelist.png`

# F03.4 — Description Accuracy

**Verdict (unchanged):** CONFIRMED_MINOR
**Banner visible at Default:** yes (INFO: BUG-REPRO panel, expanded, 2nd panel under SOLVER wrapper)
**Inaccuracies found:** 3

## Inaccuracies fixed

1. **"▸ chevron"** → **"▾ chevron"** — outline starts fully expanded so the chevron next to `SECOND_…` is `bi-chevron-down` (▾), not right-pointing (▸). A user following the old text would look for the wrong glyph. Also changed "next to" → "to the left of" (chevron is on the row's left edge).
2. **`observed` missing the collapse detail** → added "chevron flips ▾→▸, `child_a`/`child_b` disappear" so the user knows exactly what "collapses" looks like on screen; tightened the no-scroll wording to "The transcript pane does **not** scroll — the finding's original 'scrolls the main transcript' claim does not apply to the inspect viewer".
3. **`extra` not in CONFIRMED_MINOR format** — old text led with "Downgraded from MEDIUM." Rewrote to lead with `**Note:** impact is minor — …` per protocol, explaining the only inspect-observable symptom is the bold highlight moving (self-corrects via `useScrollTrack`) and that the scroll-jump is **scout-only**. Kept the MEDIUM→LOW downgrade note at the end.

Also trimmed the title (dropped the parenthetical "(scroll claim is false in inspect)" — now covered in `observed`/`extra`).

## Re-verification

Walked the rewritten `where_to_look` steps against the regenerated `.eval`:
- Step (1) click `FIRST` label → row gains `_selected_` (bold) ✓
- Step (2) `SECOND` has a ▾ chevron-down ✓; clicking it → `SECOND` gains `_selected_`, `FIRST` loses it, chevron flips to `chevron-right`, `child_a`/`child_b` removed from outline ✓
- Transcript `scrollTop` unchanged (478→478) ✓

## Before/after screenshots

- before: artifacts/accuracy/F03.4-before.png
- after: artifacts/accuracy/F03.4-after.png

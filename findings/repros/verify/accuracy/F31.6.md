# F31.6 — Description Accuracy

**Verdict (unchanged):** FALSE_POSITIVE — will be deleted
**Banner visible at Default:** no → **fixed** (was hidden in collapsed `SUB-AGENT: BANNER` from `chain()`; inlined `emit_bug_banner` into single `repro_solver()`)
**Inaccuracies found:** 4

## Inaccuracies fixed
1. **Missing FALSE_POSITIVE lead-in** → title now starts `**✅ FALSE POSITIVE — NOT A BUG.** Footer correctly shows "N Samples". config.epochs is always defaulted to ≥1 by eval_config_defaults() so the || 0 branch is unreachable.`
2. `observed`: "M is `0` despite there being 3 samples" → "Footer shows `3 Samples`. JSON shows `"epochs": 1`. `evalSampleCount = 3 * (1 || 0) = 3` — correct." — the footer never showed 0; it shows the correct count.
3. `observed`: "Confirm in JSON tab that `eval.config.epochs` is `null`" → removed; JSON tab actually shows `"epochs": 1`. New `where_to_look` directs the user to verify it is `1` (not `null`).
4. `where_to_look`: "footer / progress text for total sample count (`SampleList` `totalItemCount` prop)" → "footer at bottom-right reads `3 Samples` (correct). Then **JSON** tab → `eval.config.epochs` is `1`" — names the actual rendered text and adds the JSON-tab check that proves the premise false.

Also: `expected` rewritten from "Default to `1`, not `0`" (implies a fix is needed) → "Same — behaviour is correct" + explanation that `eval_config_defaults()` always writes `epochs ≥ 1`. `extra` now records the original (false) claim and notes the residual `|| 0` vs `|| 1` code-smell.

## Verification of new description
- Samples-tab footer: `3 Samples` ✅ (matches `where_to_look` / `observed`)
- JSON tab: `"epochs": 1` ✅ (matches `where_to_look` / `observed`)
- Transcript (Default filter): `INFO: BUG-REPRO` panel is first event, expanded, no `SUB-AGENT` wrappers ✅

## Before/after screenshots
- before: artifacts/accuracy/F31.6-before.png
- after: artifacts/accuracy/F31.6-after.png
- after (Samples tab footer): artifacts/accuracy/F31.6-after-samples.png

# F90.5 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel, expanded, not inside collapsed sub-agent)
**Inaccuracies found:** 4

## Inaccuracies fixed

1. `where_to_look`: "click the **Error** tab (red)" → "click the **Error** tab" — the tab button is rendered in black (`rgb(0,0,0)`), not red; there is no red styling on the sample Error tab.
2. `where_to_look`: "Search the rendered card for the string `F90.5_…`" → "The card is titled **SAMPLE ERROR** and its body contains exactly one element: the ANSI traceback box" — replaced a vague search instruction with the concrete, verifiable structure (header label + single `_ansiDisplayContainer_*` child, both confirmed in DOM).
3. `observed`: "appears only as the last line of the traceback — if a traceback were empty or the message carried extra context not in the frames, it would be lost" → "**is** visible — but only as the final `RuntimeError: …` line *inside* the ANSI traceback … `sample.error.message` is never read" — per audit directive, clarified that the message **is** on screen (inside the traceback), removed the confusing hypothetical, and stated the actual gap (no separate panel; `error.message` field unused).
4. `observed`/`expected`: "Contrast with the **log-level** Error tab (`TaskErrorPanel`) which renders …" → moved to `expected` with caveat "(No log-level Error tab exists in *this* log to compare against — the task succeeds because `fail_on_error=False`; the comparison is in source.)" — the original text invited the user to open a log-level Error tab that does not exist in this `.eval` (workspace tabs are SAMPLE/TASK/MODELS/INFO/JSON only).

Also added to `extra`: "**Note:** impact is minor — for ordinary Python exceptions the message text still reaches the screen via the traceback's last line, so this is a UI-consistency gap rather than data loss in the common case." (per per-finding/F90.5.md severity recommendation).

## Re-verification

- Error tab button exists, label `ERROR` ✓
- Card header text `SAMPLE ERROR`, header-icon span has `_headerIconEmpty_` class ✓
- Card body has exactly 1 direct child: `_ansiDisplayContainer_*` ✓
- Sentinel `F90.5_ERROR_MESSAGE_SHOULD_BE_VISIBLE` present in tab text; stripping the ANSI block leaves only `'SAMPLE ERROR\n'` (sentinel not outside traceback) ✓
- Log-level workspace tabs: SAMPLE/TASK/MODELS/INFO/JSON — no log-level Error tab ✓

## Before/after screenshots

- before: artifacts/accuracy/F90.5-before.png
- after: artifacts/accuracy/F90.5-after.png

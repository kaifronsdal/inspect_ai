# F90.3 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel is first event, expanded)
**Inaccuracies found:** 3

## Inaccuracies fixed

1. **"expand the Score event at the end"** → **"scroll to the SCORE event panel at the bottom (already expanded, not collapsible)"** — the SCORE event panel has no chevron (`bi-chevron-right`/`-down` count = 0); it cannot be expanded or collapsed. Telling the user to "expand" it sends them hunting for a control that doesn't exist.

2. **"its Value row"** → **"its Score row"** — the panel's rows are labeled TARGET / ANSWER / EXPLANATION / SCORE. There is no row labeled "Value". DOM: `<div class="text-style-label">Score</div><div class="">true</div>`.

3. **"sample-detail header score pill"** → **"the Score column in the sample header (top right, next to Id/Input/Target/Answer)"** — the badge lives in the `Score` column of the `#sample-heading-inline-sample-display` grid, alongside Id/Input/Target/Answer columns. "score pill" was vague about *where* to look; the new text names the column and its neighbours so the user can find it immediately.

Also tightened `observed` to match: "coloured circle badge" → "green oval badge" (it's a 40×30px green pill, CSS class `_circle_..._green_`); "ScoreEvent panel" → "transcript SCORE event panel's Score row" (names the exact row).

## Re-verification

- Banner visible at top under Default filter: ✅
- (1) Header `Score` column shows `<span class="_circle_ ... _green_">true</span>`: ✅
- (2) Scoring tab shows 2× green oval badges: ✅
- (3) Transcript → last panel title renders `SCORE`, no chevron, row labels = TARGET/ANSWER/EXPLANATION/SCORE, Score row = bare `<div class="">true</div>`: ✅
- Sample list (log Samples tab) shows green badge: ✅ (referenced in `observed`)

## Before/after screenshots

- before: artifacts/accuracy/F90.3-before.png
- before (panel close-up): artifacts/accuracy/F90.3-before-scorepanel.png
- after: artifacts/accuracy/F90.3-after.png

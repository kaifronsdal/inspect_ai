# F31.2 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel is first event, expanded)
**Inaccuracies found:** 4

## Inaccuracies fixed

1. **`where_to_look`: "Plan card" → "SUMMARY card"** — the Info-tab card is
   titled `SUMMARY` in the UI; there is no card labelled "Plan".
2. **`where_to_look`: "Scorers column" → "SCORER column"** — the column
   heading is singular (`SCORER`) when there is one scorer; "Scorers" does
   not appear on screen.
3. **`expected`: "`includes` shows `ignore_case: true`" — was FALSE.** The
   original repro used `includes()` with no explicit args, so
   `results.scores[0].params == {}` and the SCORER column showed the bare
   name only (per-finding evidence: `'ignore_case' present? False`). The
   asymmetry the description told the user to look for was not observable.
   **Fix:** changed task to `scorer=includes(ignore_case=True)` so the param
   is recorded; SCORER column now visibly renders `IGNORE_CASE true` under
   `includes`, and the description now points at it as the contrast.
4. **Rendered-text mismatch: `ignore_case: True` → `IGNORE_CASE true`** —
   matched the description to the on-screen rendering (CSS-uppercased key,
   JSON-lowercase boolean).

Also moved the asymmetry claim from `expected` into `observed` (it is now an
observable fact on the page, not a prediction) and tightened `expected` to
the root-cause statement only.

## After-state evidence

```
SOLVERS column inner_text: 'SOLVERS\nparameterised_solver'
SCORER  column inner_text: 'SCORER\n includes\nIGNORE_CASE\n\ntrue'
Info-tab body grep: 'F31.2_SHOULD_BE_VISIBLE' present? False   ← bug confirmed
                    'IGNORE_CASE'             present? True    ← contrast now visible
log: plan.steps[0].params      = {my_param: 'F31.2_SHOULD_BE_VISIBLE', threshold: 0.777, retries: 3}
     results.scores[0].params  = {ignore_case: True}
```

## Before/after screenshots
- before (transcript): artifacts/accuracy/F31.2-before.png
- before (info tab):   artifacts/accuracy/F31.2-before-info.png
- after  (info tab):   artifacts/accuracy/F31.2-after.png

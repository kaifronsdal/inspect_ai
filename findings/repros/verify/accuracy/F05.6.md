# F05.6 — Description Accuracy

**Verdict (unchanged):** CONFIRMED_MINOR
**Banner visible at Default:** yes (already inline in solver, no chain() problem)
**Inaccuracies found:** 5

## Inaccuracies fixed

1. **observed** "has **only one tab (Summary)**" → "has **NO sub-tab pills** (no tab strip rendered at all — the nav container is empty)" — user-caught: there are zero pills, not one. `EventPanel` only renders the pill strip when ≥2 children; with one child the `<div class="_navs_...">` is empty. DOM probe: `subtabs == []`.
2. **observed** "rendered inline at the bottom of Summary" → "renders inline below the Provenance section, unlabeled" — there is no visible "Summary" label/tab on screen; the user sees it appended after the PROVENANCE block with no heading.
3. **observed** added explicit contrast clause: "Contrast: the **Score** panel below DOES have `EXPLANATION | METADATA` pills" — names the actual pills (not "a Metadata tab").
4. **where_to_look** "expand the **Score Edit** event → look at the panel's tab strip" → "**Edit Score** panel → look at the top-right of the header (where sub-tab pills go)" — panel title is `EDIT SCORE` (not "Score Edit"); and there is no tab strip to "look at", so point the user at where it *would* be.
5. **expected** "A separate Metadata tab alongside Summary — exactly like ScoreEventView" → "Two sub-tab pills `SUMMARY | METADATA` … mirroring how ScoreEventView puts metadata behind a `METADATA` pill" — the Score panel's pills are `EXPLANATION | METADATA`, not Summary-anything; reworded so the comparison can't be misread.

Plus:
- **extra**: prepended `**Note:** impact is minor — the metadata IS fully visible, just appended unlabeled instead of tabbed. No data loss; cosmetic / consistency only.` (CONFIRMED_MINOR requirement). Expanded root-cause to explain why zero pills render (≥2-child gate).
- **MARKER sentinel** in the edit metadata: "…not inline at the bottom of Summary" → "…not appended unlabeled below Provenance" (matches on-screen reality).

## Before/after screenshots
- before: artifacts/accuracy/F05.6-before.png
- after: artifacts/accuracy/F05.6-after.png

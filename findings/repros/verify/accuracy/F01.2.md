# F01.2 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel expanded near top — no fix needed)
**Inaccuracies found:** 1

## Inaccuracies fixed

1. `where_to_look`: "expand **Model Call: mockllm/model**" → "in the **Model Call: mockllm/model** event, click the **Tools** sub-tab → look at the **Tool Choice** row at the bottom" — the Model Call panel has **no collapse chevron** (`i.bi-chevron-right` count = 0, `i.bi-chevron-down` count = 0); it is rendered already-open and is not expandable. Telling the user to "expand" it sends them hunting for a control that doesn't exist. Also added "at the bottom" since the Tool Choice row sits below the two tool-description rows.

## Verified accurate (no change)

- Transcript tab name ✓
- Event title "Model Call: mockllm/model" ✓
- Sub-tab pill set is SUMMARY / ALL / TOOLS — "Tools" exists and is clickable ✓
- "Tool Choice" label present (DOM text `Tool Choice`, rendered `TOOL CHOICE`) ✓
- `observed`: `<code>` inner_text is exactly `` `$my_forced_tool()` `` — backtick, `$`, name, `()`, backtick ✓
- `expected` ✓
- `extra` note re: `tools.length > 1` gating (F04.2) — Tools sub-tab does appear with the two synthetic ToolInfo entries ✓

## Before/after screenshots

- before: artifacts/accuracy/F01.2-before.png
- after: artifacts/accuracy/F01.2-after.png

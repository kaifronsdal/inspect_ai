# F11.3 — Description Accuracy

**Verdict (unchanged):** CONFIRMED_MINOR
**Banner visible at Default:** yes (already inside main solver — no chain() fix needed)
**Inaccuracies found:** 4

## Inaccuracies fixed

1. **observed:** "renders as the literal text `{\"type\":\"image\",\"image\":\"data:image/png;base64,iVBOR…\"}`" → "renders as a **key/value tree** — three rows `type: image` / `image: <tiny thumb>` / `detail: auto` (the image is still visible, just as the value of the `image:` row)" — the stringify'd JSON is re-parsed by `ToolTextOutput` → `JsonMessageContent` and shown as a RecordTree, not as raw JSON text. Per-finding DOM evidence: `inner_text='…type:\nimage\nimage:\ndetail:\nauto'`, `<img>` present inside the `image:` row, no `{"type":"image"` literal anywhere in the panel.
2. **where_to_look:** "expand the two **Tool** events" → "compare the two **Tool** panels below … Both are already expanded — no clicking needed" — neither Tool panel has an expand chevron (DOM: `bi-chevron-right`/`bi-chevron-down` count = 0); content renders inline.
3. **title:** "JSON.stringify'd instead of rendered as `<img>`" → "renders as a key/value RecordTree instead of a plain `<img>`" — describes what the user actually sees rather than the internal code path.
4. **extra:** restructured to lead with `**Note:** impact is minor — this shape is **synthetic-only**. Real tool execution (call_tools, _call_tools.py:219-236) always list-wraps a bare Content* before writing ToolEvent.result, so in-tree code never produces it.` — required by CONFIRMED_MINOR protocol; previous note buried this and framed it as "rare in the wild" rather than unreachable from real tools. Also added explanation of why a RecordTree appears (stringify → re-parse) and the suggested one-line fix.

## Before/after screenshots
- before: artifacts/accuracy/F11.3-before.png
- after: artifacts/accuracy/F11.3-after.png

## Re-verification (after regenerate)

- Banner `INFO: BUG-REPRO` visible & expanded at top under Default filter: ✅
- `TOOL: SCREENSHOT_BARE` panel shows `type: image / image: <img> / detail: auto` rows (matches new `observed`): ✅
- `TOOL: SCREENSHOT_LIST` panel shows single `<img>` only (matches new `observed`): ✅
- No expand chevrons on either Tool panel (matches new `where_to_look`): ✅
- `**Note:** impact is minor` present in banner extra: ✅

# F20.1 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel expanded inside SOLVER span; no chain() problem)
**Inaccuracies found:** 3

## Inaccuracies fixed

1. **observed** — "The header shows Input / Target / Answer / Score" → "The header shows only **ID / Input / Target**"
   *Why:* Actual header labels are `['ID', 'INPUT', 'TARGET']` (3-column grid). There is no Answer column and no Score column — this task has no scorer. The old text named two columns that don't exist and omitted the ID column that does.

2. **observed** — "Compare with the sample-list row, which *does* show a Limit column." → removed; replaced with "see the *Message Limit Exceeded* event below, and `limit.type: \"message\"` in the JSON tab"
   *Why:* This is a single-sample log, so the viewer renders it as `inline-sample-display` with no ag-grid sample list. The comparison step the user was told to perform is impossible against this `.eval`. Replaced with two references that *are* on screen and confirm the limit data exists.

3. **extra** — "The sample list (one level up) allocates a Limit column … so the column is reserved in the header but always empty." → "Source asymmetry: the multi-sample list view (`list/columns.tsx`) reads `limit` directly off `SampleSummary` with no guard, while this detail header gates the same field behind `isEvalSample()` (`SampleSummaryView.tsx:76`). This repro is single-sample so there is no list grid to compare against — verify the data exists via the JSON tab."
   *Why:* (a) "one level up" implies a navigable list that doesn't exist here; (b) "the column is reserved in the header but always empty" is false — the header grid-template has exactly 3 columns, no reserved-but-empty Limit slot. Rewrote as a source-code asymmetry note with an explicit caveat that the list isn't visible in this repro.

## Re-verification

- Header labels: `['ID', 'INPUT', 'TARGET']` — matches new `observed` text exactly
- Tab strip: TRANSCRIPT / MESSAGES / SCORING / METADATA / JSON — `where_to_look` path valid
- `MESSAGE LIMIT EXCEEDED` event visible in transcript — referenced pointer works
- JSON tab contains `"limit": { "type": "message", "limit": 2 }` — referenced pointer works
- ag-grid sample list: absent (confirmed) — new `extra` correctly warns of this

## Before/after screenshots

- before: artifacts/accuracy/F20.1-before.png
- after: artifacts/accuracy/F20.1-after.png

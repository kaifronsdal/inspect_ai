# F01.3 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel, expanded, first event)
**Inaccuracies found:** 3

## Inaccuracies fixed

1. "expand the **Score Edit** event" → "scroll to the **Edit Score** event" — the panel title in the viewer is **Edit Score** (not "Score Edit"), and the panel has **no collapse chevron** (it is always rendered fully open), so "expand" was a misleading instruction.
2. "(appears after the model turn, before scoring)" → "(between the Model Call and Score events) → look at the **Updated Values** grid" — replaced vague "before scoring" with the actual surrounding panel titles, and added the **Updated Values** section header so the reader's eye lands on the exact grid where the bug shows.
3. "should be hidden (or show '—')" → "should be hidden (or show `[unchanged]`)" — the existing sentinel handling for `answer` renders the literal `[unchanged]` placeholder, not an em-dash; aligned the expectation with what the codebase already does for the guarded field.

Also tightened `observed` to lead with "Under *Updated Values*, …" so it anchors on the visible section header.

## Re-verification

- Panel title `EDIT SCORE` matches `where_to_look` ✓
- Panel sits between `MODEL CALL` and `SCORE` ✓
- `UPDATED VALUES` grid present with rows VALUE=`UNCHANGED`, ANSWER=`edited-answer-only`, EXPLANATION=`UNCHANGED` ✓
- No expand/collapse step required ✓

## Before/after screenshots

- before: artifacts/accuracy/F01.3-before.png
- after: artifacts/accuracy/F01.3-after.png
- panel close-up: artifacts/accuracy/F01.3-before-panel.png

# F80.10 — Description Accuracy

**Verdict (unchanged):** CONFIRMED_MINOR
**Banner visible at Default:** yes (INFO: BUG-REPRO is first panel, expanded — no fix needed)
**Inaccuracies found:** 5

## Inaccuracies fixed

1. **Title** "formatPrettyDecimal / formatDecimalNoTrailingZeroes collapse 1.234e-7 to '0.000'" → "formatDecimalNoTrailingZeroes collapses 1.234e-7 to '0.000000' (discontinuity at 1e-6)" — per-finding analysis showed the headline `0.000` (`formatPrettyDecimal`) is ordinary 3-dp rounding (any value < 0.0005 → `0.000`), *not* the toString() bug. The real defect is the sample-row `0.000000`. Title now names the right function and the right output.
2. **where_to_look** listed the title-bar headline metric and log-list score column as primary evidence → removed; now points only at **Samples tab → Score column** and the dialog-header score (both verified to render `0.000000` via `formatDecimalNoTrailingZeroes`). The headline/log-list surfaces are no longer presented as where the bug lives.
3. **observed** "Headline metric / log-list show `0.000` … Both are indistinguishable from a true zero" → reframed so `0.000000` in the Score column is THE bug, with the discontinuity spelled out (`0.0001234` → `"0.0001234"` full-precision, but `1.234e-7` → `"0.000000"`). The misleading implication that headline `0.000` demonstrates the defect is gone.
4. **observed** "shows `0.000000` (or similar)" → dropped "(or similar)"; DOM confirms it is exactly `0.000000`.
5. **extra** previously only described root cause and ended with "→ `'0.000'`" (wrong output for the surface that matters) → now (a) ends the root-cause chain at `toFixed(6)` → `'0.000000'`, (b) adds a **Not the bug** paragraph explaining the headline `MEAN 0.000` is normal `formatPrettyDecimal` rounding, and (c) adds the required CONFIRMED_MINOR **Note:** — impact is minor because sub-1e-6 scores are rare and the raw value remains visible in the Scoring/JSON tabs.

## Re-verification

- Samples tab Score column: `0.000000` ✓
- Dialog header top-right: `0.000000` ✓ (3 exact-text matches in dialog DOM)
- Title bar: `MEAN 0.000` ✓ (referenced only in the "Not the bug" note)
- Scoring tab shows raw `1.234e-7` ✓ (backs the "raw value still visible" minor-impact claim)

## Before/after screenshots
- before: artifacts/accuracy/F80.10-before.png
- after: artifacts/accuracy/F80.10-after.png

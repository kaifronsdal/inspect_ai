# F02.1 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel expanded under `SOLVER: EMIT_SANDBOX_EVENTS`; no chain() problem)
**Inaccuracies found:** 3

## Inaccuracies fixed

1. **`where_to_look` omitted the Debug filter** → added "**switch the Events filter to Debug first** (funnel button top-right: `Events: Default` → `Debug`) — sandbox events are hidden under the Default filter." — Under the Default filter, sandbox events are excluded entirely (`kDefaultExcludeEvents`), so a user following the old instructions would see zero sandbox rows and conclude the repro is broken.
2. **`where_to_look` said "expand the solver"** → removed. Under the Debug filter there is no `SOLVER:` panel at all — every event renders as a flat top-level sibling at x=121. There is nothing to expand. Replaced with "Scroll past the model call" which is the actual action needed.
3. **`observed` said "under the solver span"** → changed to "render as flat top-level siblings". Under Debug the solver span wrapper is not rendered; the three `SANDBOX:` panels sit at the same indent as `INIT` / `MODEL CALL` / `STATE UPDATED`, not nested under anything.

Also tightened the panel-name references to match what's on screen (`SANDBOX: EXEC` / `SANDBOX: READ_FILE` instead of bare `exec` / `read_file`).

## Re-verification

- `Events: Default` button present → click → `Debug` option present → click → 7 flat panels at x=121 including exactly `['SANDBOX: EXEC', 'SANDBOX: EXEC', 'SANDBOX: READ_FILE']`, no `Sandbox Events` group panel. Every step in the new `where_to_look` works as written.

## Before/after screenshots

- before: artifacts/accuracy/F02.1-before.png
- after: artifacts/accuracy/F02.1-after.png
- after (Debug filter, sandbox panels visible): artifacts/accuracy/F02.1-after-debug.png

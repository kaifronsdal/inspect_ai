# F03.3 — Description Accuracy

**Verdict (unchanged):** FALSE_POSITIVE
**Banner visible at Default:** no → **fixed** (moved `emit_bug_banner(DESC)` *inside* the `supervisor` span — the viewer treats the single top-level agent span as the transcript root, so the InfoEvent emitted *outside* it was not rendered at Default. Now `INFO: BUG-REPRO` is panel #0, expanded.)
**Inaccuracies found:** 5

## Inaccuracies fixed

1. **where_to_look:** "**Timeline** tab (not Transcript)" → "**Transcript** tab → timeline swimlane panel at the very top" — there is no `Timeline` tab; sample tabs are `Transcript / Messages / Scoring / Metadata / JSON`. The swimlane is the pinned panel at the top of the Transcript tab.
2. **where_to_look:** "Hover the … swimlane row to see its breadcrumb trail" → "**click** the … row label. The breadcrumb header (top-left of the swimlane) updates" — hovering does nothing; the breadcrumb only updates when a row is *selected* by click.
3. **where_to_look:** added the missing step "click the chevron on `my_pkg/my_module/weird_agent` to expand it" — `grandchild` is collapsed under `weird_agent` on load, so the deepest (3-segment) breadcrumb is unreachable without expanding first.
4. **observed/expected:** `supervisor › …` → `main / …` — the outer `supervisor` span becomes the timeline root, which the swimlane labels `main`; no row or breadcrumb segment is ever called `supervisor`. Divider glyph is `/`, not `›`.
5. **observed (verdict flip):** "Breadcrumb trail has missing/phantom segments … expand/collapse may mis-associate the parent" → "Breadcrumb shows exactly **3** segments … no phantom `my_pkg`/`my_module` segments; no real ancestor missing. Collapsing `main` correctly hides the slashed child rows. **This is correct behaviour.**" — the original observed claim is the false-positive; rewrote to state what actually renders and why it is correct (the `if (layout)` guard at `TimelineSwimLanes.tsx:63` drops phantom prefix lookups).

## Also tightened (not inaccuracies, just precision)

- **title:** now leads with `**✅ FALSE POSITIVE — NOT A BUG.**` per protocol §4.
- **expected:** restated as "same as observed" + one-line root-cause (guard skips misses).
- **extra:** replaced the as-if-bug source walkthrough with a "why the original finding is wrong" explanation; kept the F03.23 cross-reference (verified still accurate — left Outline tree labels both nested agents `my_module`).
- **solver comment:** updated the inline `# … → breadcrumb gap; ancestor-collapse mis-parses` comment to reflect the guard-drops-it reality.

## Re-verification

- Panel #0 = `INFO: BUG-REPRO`, visible, expanded, leads with ✅ FALSE POSITIVE ✅
- Swimlane present at top of Transcript tab; `weird_agent` row has Expand chevron ✅
- After expand → click `grandchild`: breadcrumb segments = `['main', 'my_pkg/my_module/weird_agent', 'my_pkg/my_module/grandchild']` (exactly 3) ✅
- Collapse `main` → 1 row remains (slashed children correctly hidden) ✅
- Outline tree at default load: `main / 1 turn / my_module / 1 turn / my_module / 1 turn` — F03.23 cross-ref accurate ✅

## Before/after screenshots

- before: `artifacts/accuracy/F03.3-before.png`
- after: `artifacts/accuracy/F03.3-after.png` (default load — banner visible)
- after (breadcrumb): `artifacts/accuracy/F03.3-after-breadcrumb.png` (grandchild selected — 3-segment breadcrumb visible)

# F05.12 — Description Accuracy

**Verdict (unchanged):** CONFIRMED_MINOR
**Banner visible at Default:** yes (INFO: BUG-REPRO panel expanded at top; no chain() fix needed)
**Inaccuracies found:** 3

## Inaccuracies fixed

1. `where_to_look`: "Transcript tab → expand the **Branch** event" → "Transcript tab → set the **Events** filter (top-right) to **Debug** → look at the **Branch** panel (it is not collapsible — the two-row grid is its full content)." — Branch events are excluded by the Default filter, so the panel does not exist until the user switches to Debug. Additionally the Branch panel has **no chevron** (`bi-chevron-*` count = 0), so "expand" is wrong; it renders its grid immediately.
2. `observed`: "Only `from_span` and `from_message` are shown" → "Only `from_span` and `from_message` rows are shown … **in the panel**." — minor tightening to match the two-row MetaDataGrid actually rendered and scope the "not anywhere" claim to the panel (the strings *are* present in the JSON tab).
3. `extra`: added the required CONFIRMED_MINOR severity note — "**Note:** impact is minor — BranchEventView follows the codebase norm: **no** event view renders `BaseEvent.metadata` except `CompactionEventView`. Branch events are also hidden by the default filter, and no public API currently sets metadata on a BranchEvent (this repro uses `transcript()._event()` directly)." This replaces the old parenthetical that implied the renderer *should* handle it without qualifying that Compaction is the outlier.

## Re-verification (Debug filter)

- Filter button reads `Events: Debug` after following `where_to_look`.
- Branch panel text: `BRANCH (0 SEC) / FROM_SPAN origin-span-id-12345 / FROM_MESSAGE origin-message-id-abcde` — exactly two rows, no chevron.
- `MARKER` / `branch_reason` absent from panel; present in sample JSON tab. Bug still reproduces.

## Before/after screenshots

- before: artifacts/accuracy/F05.12-before.png (Default filter — no Branch panel visible)
- after: artifacts/accuracy/F05.12-after.png (Debug filter — banner + Branch panel with two-row grid)

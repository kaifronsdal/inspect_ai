# F04.7 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** no → **fixed**
**Inaccuracies found:** 4

## Inaccuracies fixed

1. **Banner hidden in collapsed sub-agent** — `solver=chain(banner(), generate())` rendered as two collapsed `SUB-AGENT: BANNER` / `SUB-AGENT: GENERATE` rows; neither the bug-repro banner nor the Model Call panel was visible at the Default filter. → Replaced with a single `repro_solver()` that calls `emit_bug_banner(DESC)` then `await generate(state)`. After: `INFO: BUG-REPRO` and `MODEL CALL: …` are both expanded under one `SOLVER: REPRO_SOLVER` wrapper.
2. **`where_to_look`: "expand Model Call"** → **"Model Call: mockllm/model panel → check both the Summary and All sub-tabs"** — after the chain fix the panel is already expanded, so "expand" is a non-step; clarified that Summary/All are *sub-tabs* of the panel (verified actual subtabs = `SUMMARY` / `ALL` / `API`).
3. **`observed`: vague "no sign of the output.error refusal text"** → made concrete: **"appears nowhere in the panel — the All sub-tab shows only Usage / Timing / Messages sections"** — matches the actual section labels on screen (no Error / Stop-Reason section exists).
4. **`expected`: "rendered in the Error section"** → **"rendered alongside the output (the existing Error section only reads `event.error`, not `event.output.error`)"** — the original implied an Error section is already present in this panel; it isn't (it only appears when `event.error` is set). Reworded so a reader doesn't go hunting for a section that doesn't exist.

Also added to `extra`: caveat that `output.error` *does* paint a red marker on the timeline strip (per the per-finding verdict's cross-check), so the claim is scoped to "never appears in the event panel itself."

## Re-verification (after regenerate)

- Event panels at Default filter: `SOLVER: REPRO_SOLVER` › `INFO: BUG-REPRO` › `MODEL CALL: MOCKLLM/MODEL (552 TOKENS, 0 SEC)` — all expanded.
- Model Call subtabs: `['SUMMARY', 'ALL', 'API']` — matches description.
- All-tab section labels: `USAGE`, `TIMING`, `MESSAGES` (+ timing sub-fields) — no `STOP REASON`, no `ERROR`. Matches `observed`.
- Sentinel `'content filtered'` (only in `output.error`, not in any prose) absent from Summary/All panel text — bug confirmed.
- JSON tab contains `stop_reason`, `"max_tokens"`, and `content filtered` — `extra` claim verified.
- Timeline strip outerHTML contains `PROVIDER REFUSAL` (red-marker tooltip) — caveat verified.

## Before/after screenshots

- before: `artifacts/accuracy/F04.7-before.png`
- after: `artifacts/accuracy/F04.7-after.png`

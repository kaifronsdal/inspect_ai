# F11.7 — Description Accuracy

**Verdict (unchanged):** CONFIRMED_MINOR
**Banner visible at Default:** no → **fixed** (was buried in collapsed `SUB-AGENT: BANNER` / `SUB-AGENT: GENERATE` spans from `chain()`)
**Inaccuracies found:** 5

## Inaccuracies fixed

1. **chain() banner-hiding** — `solver=chain(banner(), use_tools(...), generate())` rendered the entire transcript as two collapsed `SUB-AGENT:` rows under the Default filter; neither the banner nor the tool event was visible without drilling in. → Replaced with a single `repro_solver()` that calls `emit_bug_banner(DESC)`, sets `state.tools`, and awaits `generate(state)` inline. Banner and `TOOL: BIG_OUTPUT` panel now both expanded at top level.
2. **observed:** "Output is shown clipped (you will NOT see `F11.7_FULL_OUTPUT_END`)" → **wrong**: Inspect uses head+tail truncation, so both `F11.7_FULL_OUTPUT_START` and `F11.7_FULL_OUTPUT_END` survive in the rendered output. Rewritten to state both sentinels are visible.
3. **observed:** implied no truncation indicator at all ("no UI footer saying *how much* was clipped" was the only acknowledgement) → reframed per verdict: the Python preamble *"The output of your call to big_output was too long to be displayed. Here is a truncated version:"* IS shown front-and-centre, so the user is told it was truncated; only the **byte counts** from `event.truncated` are missing from viewer chrome.
4. **observed:** "`truncated` field (≈ `[2050, 200]`)" → actual value is `(2054, 200)`. Corrected.
5. **where_to_look:** "expand the **Tool** event for `big_output`" → after the chain() fix the panel is already expanded; reworded to "the **Tool: big_output** event panel (expanded by default below this banner)" to match the on-screen title `TOOL: BIG_OUTPUT`.

## Added (CONFIRMED_MINOR handling)

- `extra` now leads with **Note:** impact is minor — the Python preamble already tells the user the output was truncated; only the precise `(raw_bytes, kept_bytes)` figures are lost. This is a UX nicety, not a confusion bug.
- `title` tightened from "is never surfaced" → "byte counts are never surfaced in viewer chrome" to scope the claim accurately.

## Re-verification (after)

Tool panel inner_text:
```
TOOL: BIG_OUTPUT (0 SEC)
turn 1/2
big_output
The output of your call to big_output was too long to be displayed.
Here is a truncated version:
<START_TOOL_OUTPUT>
F11.7_FULL_OUTPUT_START >>> 0123…6789 <<< F11.7_FULL_OUTPUT_END
<END_TOOL_OUTPUT>
```
- Banner `INFO: BUG-REPRO` visible & expanded at Default filter ✓
- `TOOL: BIG_OUTPUT` panel expanded by default ✓
- Both sentinel markers present ✓
- Preamble present ✓
- No `2054` / `200 of` byte-count chrome anywhere in panel ✓ (bug stands)
- JSON tab contains `truncated` field ✓

## Before/after screenshots
- before: artifacts/accuracy/F11.7-before.png
- after: artifacts/accuracy/F11.7-after.png

# F05.1 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel expanded inside the solver span — no `chain()` problem)
**Inaccuracies found:** 4

## Inaccuracies fixed

1. **`where_to_look` omitted the Debug filter requirement.** Old: "Transcript tab → expand the State Updated event …". At the Default filter only `SOLVER: MUTATE_NESTED_METADATA` + `INFO: BUG-REPRO` are rendered — there is **no State Updated panel**. New text leads with "**⚠️ REQUIRES DEBUG FILTER** — … click the **Events: Default** dropdown (top right) → select **Debug**".
2. **"(after the solver)" was wrong.** At the Debug filter the solver wrapper span is gone; the panel order is `INIT` → `INFO: BUG-REPRO` → `STATE UPDATED`. Replaced with "scroll to the **State Updated** panel (below this INFO panel)".
3. **"look at the Diff / Changes view" was wrong.** The State Updated panel has **no sub-tabs** (`button[role="tab"]` count = 0); the jsondiffpatch tree is the panel body itself. Replaced with "Its body is a JSON diff tree — there are no sub-tabs". Also dropped "expand" since the panel renders already-expanded.
4. **`observed` mis-described the rendered tree.** Old: "…phantom top-level `LOOK_HERE` key (sibling of `metadata`), and `metadata.LOOK_HERE` is shown as an empty object `{}` on both sides." Actual DOM: top-level keys = `['LOOK_HERE']`, all keys = `['LOOK_HERE', 'nested_key']` — **`metadata` does not appear anywhere** (jsondiffpatch elides it because it is identical-empty on both sides). New text: "The diff tree is `{ LOOK_HERE: { nested_key: ~~BEFORE~~ AFTER } }` — `LOOK_HERE` is rendered as the **top-level / root key**. The `metadata` key does **not appear anywhere** in the diff." The mechanism behind the elision was moved into `extra` so the root-cause story stays intact.

Minor: tightened the JSON-tab cross-check hint to "search for `\"event\": \"state\"`" (there is no clickable "state event" in the JSON tab — it is one flat document).

## Re-verification (all navigation steps in new `where_to_look` exercised)

- Default filter → banner visible, no State panel ✓
- Click **Events: Default** → dropdown opens, **Debug** option present ✓
- Select Debug → button reads `Events: Debug`, `STATE UPDATED` panel appears below `INFO: BUG-REPRO` ✓
- Panel has no sub-tabs; body is `.jsondiffpatch-delta` with top-level `data-key="LOOK_HERE"` only, `metadata` absent ✓
- `observed` now matches the rendered tree exactly ✓

## Before/after screenshots

- before: `artifacts/accuracy/F05.1-before.png` (Default filter — no State panel visible)
- before (debug + panel close-up): `artifacts/accuracy/F05.1-before-debug.png`, `artifacts/accuracy/F05.1-before-state-panel.png`
- after: `artifacts/accuracy/F05.1-after.png`

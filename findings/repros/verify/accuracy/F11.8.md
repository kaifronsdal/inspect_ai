# F11.8 — Description Accuracy

**Verdict (unchanged):** REPRO_BROKEN (scout-only — source-level bug confirmed; unreachable in inspect viewer)
**Banner visible at Default:** yes (INFO: BUG-REPRO panel expanded inside SOLVER span)
**Inaccuracies found:** 4

## Inaccuracies fixed

1. **Title** — "Standalone tool-message path filters content to text+image only" → "**⚠️ SCOUT-ONLY — does not reproduce in inspect viewer.** The ContentData sentinel below IS visible — inspect uses the collapsed path. Buggy filter only runs when `collapseToolMessages=false` (apps/scout only)." — Repro is being deleted; banner must lead with scout-only warning so nobody hunts for a bug that isn't visible here.
2. **Where to look** — "Messages tab with `collapseToolMessages` OFF → the `data_tool` result row" → "Messages tab, row 2 — the `data_tool` result under the assistant turn. There is **no** `collapseToolMessages` toggle in inspect; the tool message always renders collapsed into the assistant turn via `ToolCallView`." — The toggle does not exist in inspect; old instruction was impossible to follow.
3. **Observed** — "Only the 'text part survives ✓' line renders. The `ContentData` block carrying `F11.8_CONTENTDATA_SENTINEL_if_missing_bug_confirmed` is gone." → "In inspect: the `data_tool` output box shows BOTH the text line `text part survives ✓` AND the ContentData key-value tree (`sentinel: F11.8_CONTENTDATA_SENTINEL_...`, `n: 42`). Nothing is dropped — the buggy standalone-render branch at `ChatMessage.tsx:121-124` is never reached." — Screenshot shows the exact opposite of the old claim: the ContentData sentinel IS rendered as a key-value tree (user-confirmed).
4. **Expected** — "`ToolOutput` already knows how to render `ContentData` — the pre-filter in `ChatMessage.tsx` should not strip it." → "n/a in inspect. In `apps/scout` (which sets `collapseToolMessages:false`), the standalone branch should not pre-filter content to `text`/`image` only — but this .eval cannot demonstrate that." — Old expected described behaviour that cannot be exercised from `inspect view`.

Also added `extra`: source-level scope note (`ChatMessage.tsx:121-124` keeps only `text|image`, shares trigger with F10.2) + explicit note that **this repro will be deleted** since the .eval cannot reach the cited code path.

**Render fix:** changed `` `text|image` `` → `` `text`/`image` `` in the Expected cell — the raw `|` was being parsed as a table-column separator and truncating the row.

## Before/after screenshots
- before: artifacts/accuracy/F11.8-before.png (transcript), artifacts/accuracy/F11.8-before-messages.png (messages — sentinel visibly present, contradicting old "Observed")
- after: artifacts/accuracy/F11.8-after.png (transcript), artifacts/accuracy/F11.8-after-messages.png (messages)

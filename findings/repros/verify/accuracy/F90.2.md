# F90.2 — Description Accuracy

**Verdict (unchanged):** CONFIRMED_MINOR
**Banner visible at Default:** yes (INFO: BUG-REPRO panel, expanded, not wrapped in sub-agent)
**Inaccuracies found:** 7

## Inaccuracies fixed

1. **Title** "rendered three different ways on one screen" → "Two sub-minute duration precision styles in one sample view" — only two formats actually appear in this repro; the third (`formatDurationShort` → `3s`) requires timeline/agent cards which this sample doesn't trigger (DOM `AgentCardView`/`TimelineMinimap` count = 0).
2. **where_to_look (1)** "sample header **Time** pill" → dropped — the sample header shows only ID/INPUT/TARGET; there is no Time pill. Replaced with the verified path: **Metadata tab → TIME card** (`Working: 2.8 sec / Total: 2.9 sec`).
3. **where_to_look (2)** "**slow-phase** span event-panel title" → dropped — the `CUSTOM: SLOW-PHASE` panel title contains no duration string. Replaced with the verified path: **Transcript → second `MODEL CALL: MOCKLLM/MODEL` panel → ALL subtab → WORKING TIME row** (`START 3 sec / END 3 sec`).
4. **where_to_look (2)** "outline tooltip" → dropped — unverified; not needed once the ALL-subtab path is given.
5. **where_to_look (3)** "Timeline view → swimlane / minimap card … → `3s`" → removed entirely — timeline does not render for this sample, so `3s` is never on screen. Mentioned in `extra` instead as a source-only third variant not exercised here.
6. **observed** "Three formatters for the same ~2.5-second span: `2.5 sec` vs `3 sec` vs `3s`" → "Two precision styles … `N.N sec` (1 decimal) vs `N sec` (rounded int). These format *different* underlying fields (`sample.working_time` vs `model_event.working_start`), so no single value is rendered two ways" — old text was wrong on count (3→2), wrong on `3s` (never appears), and wrong on "same span" (per-finding analysis showed (A) and (B) are distinct log fields that happen to be ~6 ms apart in this repro). Also dropped the unobservable "trailing `0 sec` for whole minutes" claim (no minute-scale durations in this log).
7. **extra** — added required CONFIRMED_MINOR note: "**Note:** impact is minor — cosmetic precision mismatch only; each log field passes through exactly one formatter…", and moved the `formatDurationShort`/timeline reference here as a not-visible-in-this-repro footnote.

Also softened literal values (`2.8 sec` → "e.g. `2.8 sec`", `3 sec` → "e.g. `3 sec`") so per-run timing jitter doesn't falsify the description.

## Re-verification

- Banner: visible at top under Default filter ✓
- Nav step (A): Metadata tab → TIME card → `Working: 2.8 sec` / `Total: 2.9 sec` ✓ (1-decimal)
- Nav step (B): Transcript → 2nd Model Call → ALL subtab → WORKING TIME → `START 3 sec / DURATION 0 sec / END 3 sec` ✓ (rounded int); panel title `(… 0 SEC)` ✓
- `observed` matches screen ✓

## Before/after screenshots
- before: artifacts/accuracy/F90.2-before.png (+ F90.2-before-metadata.png, F90.2-before-timing.png)
- after: artifacts/accuracy/F90.2-after.png (+ F90.2-after-metadata.png)

# F02.5 — Description Accuracy

**Verdict (unchanged):** CONFIRMED_MINOR (unreachable on real `handoff()` logs)
**Banner visible at Default:** yes (top-level `INFO: BUG-REPRO`, expanded, no ancestor span)
**Inaccuracies found:** 6

## Inaccuracies fixed

1. **`where_to_look`:** "Transcript tab → expand the solver → find …" → "Transcript tab (Default filter). Find the `TOOL: TRANSFER_TO_SUBAGENT` panel and compare its left edge to the `AGENT: AGENT_SPAN_SHOULD_BE_INDENTED_UNDER_TOOL_NOT_FLUSH_WITH_IT` panel directly below it." — There is no solver panel to expand; events render at top level. Also updated panel names to match the exact on-screen titles.
2. **`observed`:** "The agent span's own children then jump **two** indent levels to the right." → "The agent's children (`INFO: REPRO`, `STEP: INNER_WORK_…`) then sit one indent step right of the agent." — DOM measurement shows children at +1 level (pad 11.2px vs 0px), not +2. The +2 prediction from source-math is not what actually renders (per-finding/F02.5.md §Analysis).
3. **`expected`:** "+1 / +1 / +1 staircase" → "+1 / +1 staircase" — minor tidy to match the two-level comparison actually described.
4. **`extra`:** "agent.children→D+2 (skip D+1)" → removed; replaced with "the first child level is over-reduced by one and the agent ends up flush with the tool" — keeps the accurate source explanation, drops the unobservable +2 claim.
5. **`extra`:** added required minor-impact note: "**Note:** impact is minor — `unwrap_handoff` does **not** fire on logs produced by current `handoff()` (the real `ToolEvent.span_id` is captured before the tool span opens, so the matcher fails). This repro hand-builds the matching shape; only legacy logs or a future `span_id` fix would hit this in practice."
6. **InfoEvent body text:** "I SHOULD be one indent right of the agent span above. Instead I am TWO indents right (depth gap)." → "I am a child of the AGENT span above. The bug is that the AGENT span is flush with the TOOL panel instead of indented under it." — the old text described a state that does not occur (it *is* one indent right of the agent).

Also fixed (non-banner): module docstring no longer claims the repro "builds the exact span/event shape that the real `handoff()` flow produces" — it now states the repro hand-builds the shape `unwrap_handoff` expects and notes real `handoff()` does not produce it. Solver docstring comment "jumps 2 indents right" → "renders one indent right".

## Re-verification

- Banner `INFO: BUG-REPRO` visible/expanded at top under Default filter: ✅
- Navigation in `where_to_look` works as written (no expand step needed; panels are top-level): ✅
- Panel names in description match screen exactly (`TOOL: TRANSFER_TO_SUBAGENT`, `AGENT: AGENT_SPAN_SHOULD_BE_INDENTED_UNDER_TOOL_NOT_FLUSH_WITH_IT`, `INFO: REPRO`, `STEP: INNER_WORK_…`): ✅
- `observed` matches DOM (`TOOL` x=472.0, `AGENT` x=472.0 → flush; children x=483.2 → +1): ✅
- Minor-impact note rendered in banner: ✅
- Stale claims absent from page (`expand the solver`, `TWO indents right`, `jump two indent levels`): ✅

## Before/after screenshots

- before: artifacts/accuracy/F02.5-before.png
- after: artifacts/accuracy/F02.5-after.png

# F31.3 — Description Accuracy

**Verdict (unchanged):** CONFIRMED (plan.name only; plan.finish half FALSE_POSITIVE)
**Banner visible at Default:** no → **fixed** (was buried in collapsed `SUB-AGENT: BANNER`)
**Inaccuracies found:** 5

## Inaccuracies fixed

1. **Banner hidden in collapsed sub-agent span** → merged the separate `banner()` step into a single `f31_3_main_step()` solver (banner + generate). With one non-empty step the viewer renders it as an expanded `AGENT:` panel instead of a collapsed `SUB-AGENT:` row, so `INFO: BUG-REPRO` is now the second visible event panel under the Default filter.
2. `where_to_look`: "Open this log → **Info** tab → Summary card" → "Close this sample → log-level **Info** tab → **SUMMARY** card" — the user is reading the banner inside the sample dialog and must close it to reach the log-level tabs; card title on screen is **SUMMARY** (uppercase).
3. `observed`: "the column header is the hard-coded literal `'Solvers'`" → "the middle column heading in the SUMMARY card is the hard-coded literal **SOLVERS**" — verified the three column subheadings are DATASET / SOLVERS / SCORER; matched on-screen casing and clarified which column.
4. `expected`: "as a row in the Task-tab info grid" → "as a row in the Task tab's TASK INFO card" — the Task tab's card is titled **TASK INFO**, not "info grid".
5. `extra`: added on-screen proof for the FALSE_POSITIVE half — "on the Info tab the SOLVERS column reads `f31_3_main_step → f31_3_finish_solver` — the finish step is right there" (previously only cited source code). Also added a minor-impact note (`Plan` is deprecated, `EvalPlan.name` defaults to `"plan"`).

## Side effect of fix #1

The SOLVERS chain on the Info tab changed from `banner → generate → f31_3_finish_solver` to `f31_3_main_step → f31_3_finish_solver` — cleaner, and the `extra` text now references the exact chain that's rendered.

## Re-verification

- Transcript (Default filter): 4 event panels — `AGENT: F31_3_MAIN_STEP`, `INFO: BUG-REPRO` (expanded, full markdown table visible), `MODEL CALL`, `SCORE`. No `SUB-AGENT` rows.
- Info tab: SUMMARY card present; subheadings = `['DATASET', 'SOLVERS', 'SCORER']`; sentinel `F31.3_PLAN_NAME_SHOULD_APPEAR_SOMEWHERE` **absent** (bug confirmed); chain = `f31_3_main_step → f31_3_finish_solver`.
- Task tab: TASK INFO card present; sentinel **absent**.
- JSON tab: sentinel **present** under `plan.name`.

## Before/after screenshots

- before: artifacts/accuracy/F31.3-before.png (transcript), artifacts/accuracy/F31.3-before-info.png (Info tab)
- after: artifacts/accuracy/F31.3-after.png (transcript), artifacts/accuracy/F31.3-after-info.png (Info tab)

# F90.14 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO is first panel, expanded)
**Inaccuracies found:** 5

## Inaccuracies fixed

1. **"sample-detail header score pill"** → removed. There is no score pill in the sample-detail header — the navbar shows only `Sample F90.14 (Epoch 1)` and a `0 sec` timing pill. The `1` value is only in the sample-list Score column (and the transcript SCORE event).
2. **"single-log Samples tab"** → **"Sample tab"**. With one sample the workspace tab is labelled `SAMPLE` (singular), not `SAMPLES`.
3. **"title-bar ResultsPanel headline metric"** → **"top-right headline metric (`MEAN` over `1.0`)"**. `ResultsPanel` is an internal component name; replaced with what the user actually sees.
4. **Surface 3 navigation: "(point viewer at the whole `logs/90-cross/` dir) → score column"** → **"go back to the root log listing → click the Samples toggle (top-right, next to Tasks/Folders)"**. The viewer is already pointed at that dir; the old text implied a separate `inspect view` invocation.
5. **Surface 3 column: "score column"** → **"`returns_one_point_zero` column"**. The multi-log Samples grid has no column headed "Score" — columns are named after scorers.

Also added: "Surfaces 1 and 2 are visible together on one screen" (the headline `1.0` and per-sample `1` sit ~400px apart on the Sample tab — the strongest demonstration).

## Re-verification

| Surface | Nav path (as now written) | Rendered |
|---|---|---|
| 1a | open log → top-right `MEAN` | `1.0` |
| 1b | root → Tasks view → Score column | `1.0` |
| 2 | log → Sample tab → Score column | `1` |
| 3 | root → Samples toggle → `returns_one_point_zero` column | `1.000` |

All paths walked via Playwright on the regenerated logs; column header `returns_one_point_zero` confirmed present.

## Before/after screenshots

- before: artifacts/accuracy/F90.14-before.png (+ -surface1b, -surface2, -surface3)
- after: artifacts/accuracy/F90.14-after.png
- after (surfaces 1+2 on one screen): artifacts/accuracy/F90.14-after-s1s2.png
- after (surface 3): artifacts/accuracy/F90.14-after-s3.png

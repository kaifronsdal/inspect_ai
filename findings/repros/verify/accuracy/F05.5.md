# F05.5 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel, expanded, first event)
**Inaccuracies found:** 3

## Inaccuracies fixed

1. **Panel name** — `where_to_look` said "expand each of the three **Score Edit** events" → "the three **Edit Score** panels → **Updated Values** section". The viewer titles the panel **Edit Score**, not Score Edit. Also dropped "expand" (panels render expanded by default — no chevron click required) and added the **Updated Values** section name so the user knows exactly which grid to inspect.
2. **Observed contents** — said "only the Explanation is shown" → "only **Answer** `[unchanged]` and **Explanation** are listed". The Updated Values grid actually renders two rows (Answer + Explanation), not one; the original text would have a user hunting for a panel that shows Explanation alone.
3. **Expected** — "Each panel should show **Value: …**" → "Each Updated Values section should show **Value: …**". Anchors the expectation to the correct section header that is visibly on screen.

## Re-verification

- 3 **Edit Score** panels present; each contains an **UPDATED VALUES** section with `ANSWER` + `EXPLANATION` rows and **no** `VALUE` row — matches new `observed` exactly.
- Banner text now references "Edit Score" and "Updated Values"; no longer contains the string "Score Edit".

## Before/after screenshots

- before: artifacts/accuracy/F05.5-before.png
- after: artifacts/accuracy/F05.5-after.png

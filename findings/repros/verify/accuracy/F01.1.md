# F01.1 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel, expanded — no chain() fix needed)
**Inaccuracies found:** 2

## Inaccuracies fixed

1. **where_to_look:** "expand the **Model Call: mockllm/model** event → stay on the **Summary** tab (default)" → "**Model Call: mockllm/model** panel (below this banner) → **Summary** subtab (selected by default)" — the Model Call panel is already expanded at the Default filter; there is no "expand" action for the user to perform. Also clarified "subtab" since SUMMARY/ALL/API are pill subtabs on the panel header, not top-level tabs.
2. **expected:** "`[system, user (⚠️ marker), assistant, output]`" → "`[system (⚠️), user, user (⚠️), assistant, output]` (5 cards)" — the input actually contains **two** user messages (the DESC text + the ⚠️ sentinel), so a fixed Summary would render 5 cards, not 4. Also marked the system message as ⚠️-bearing since it carries a sentinel too.

## Also tightened (not inaccuracies, just precision)

- **observed:** rephrased to "only two ASSISTANT cards" to match exactly what renders (both cards are labelled ASSISTANT).
- **extra:** "click the **All** tab" → "click the **All** subtab on the same panel" — disambiguates from the top-level sample tabs.

## Re-verification

- Subtabs on Model Call panel: `['SUMMARY', 'ALL', 'API']` — Summary active by default ✅
- Summary tab: 2 ASSISTANT cards, ⚠️ sentinel absent ✅
- All tab: SYSTEM + USER cards present, ⚠️ sentinel present ✅
- Every navigation step in `where_to_look` works as written ✅

## Before/after screenshots

- before: artifacts/accuracy/F01.1-before.png
- after: artifacts/accuracy/F01.1-after.png

# F11.4 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel expanded at top of Transcript, inside SOLVER span but visible — no fix needed)
**Inaccuracies found:** 4

## Inaccuracies fixed

1. **where_to_look** — "the `plain_text_tool` call's INPUT block" → "message #2 (the assistant tool call) → the block titled `format='text' custom view`, just above the grey tool-output box." — There is no "INPUT" label anywhere on the tool-call view; the block is identified on screen by its `ToolCallContent.title` (`format='text' custom view`) with a scissors icon.
2. **observed** — "renders as a large H1 heading" → "renders as a bold heading (the `#` is consumed — it's an `<h1>` in the DOM, though styled small)". — The `<h1>` is styled at `text-size-small` in this context, so it is visually the same size as the bold line below it; calling it "large" misleads the reader into looking for a giant heading that isn't there.
3. **observed** — added "line 3 is a `•` bullet" — the bullet list is the most visually unambiguous symptom on screen and was missing from the description (the old text skipped from "second line" to "fourth").
4. **PLAIN_TEXT_BODY sentinel** — "If the line above is a giant heading and this line is bold" → "If you see a bullet point and a blue hyperlink above (instead of a literal hyphen and literal square-bracket text)". — The old sentinel was factually wrong: "the line above" the sentinel is the link (not the heading), the sentinel line itself is *not* bold, and nothing renders as "giant". Rewritten to reference the two most obvious visual symptoms, with no markdown sigils in the sentinel itself (a first attempt using `'[not a link](...)'` got parsed into a second spurious link).

## Re-verification (after)

DOM in `[class*="toolView"]`: `h1`=1, `strong`=1, `ul>li`=1, `a[href="http://example.invalid"]`=1 — every element claimed in the new `observed` text renders. Navigation path checked: Messages tab exists; message #2 contains the title `format='text' custom view`; grey tool-output box is directly below it.

## Before/after screenshots

- before (transcript banner): `artifacts/accuracy/F11.4-before.png`
- before (messages tab): `artifacts/accuracy/F11.4-before-messages.png`
- before (toolView only): `artifacts/accuracy/F11.4-before-toolview.png`
- after (messages tab): `artifacts/accuracy/F11.4-after.png`

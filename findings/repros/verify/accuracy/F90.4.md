# F90.4 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel expanded near top of Transcript; `emit_bug_banner` already inside `maybe_raise()` solver body — no chain() problem)
**Inaccuracies found:** 3

## Inaccuracies fixed

1. **`where_to_look`: "left sidebar → 'Samples' panel"** → **"click the Samples view-mode button at the top right (next to Tasks / Folders)"** — there is no left sidebar in the viewer. The multi-log Samples grid is reached via a `Tasks | Folders | Samples` view-mode toggle in the **top-right** corner of the directory-listing toolbar. Clicking it navigates to `#/samples/`. Verified by Playwright click → URL `http://127.0.0.1:7855/#/samples/`.
2. **`where_to_look`: "or the top-level Samples tab that aggregates across logs"** → dropped — there is no such tab; the Samples button *is* the only entry point. Replaced with explicit column reference: "look at its **Status** column".
3. **`observed`: "Open the single-log Samples tab for log A"** → **"click the `F90.4_multilog_grid_log_status_A` log in the directory listing → Samples tab"** — made the comparison navigation concrete. Also added that the per-sample error *is* present in the rightmost **Error** column (scroll right), matching what's actually on screen and the per-finding evidence.

## Claims re-verified against new logs

- Multi-log grid headers: `# | Task | Model | Sample ID | Epoch | Input | Status | Log File | Target | … | Error` ✓
- Row `F90.4-errored`: Status cell = `success` (plain text) ✓ — bug still reproduces
- Row `F90.4-ok`: Status = `success` ✓
- Rightmost `Error` column carries `RuntimeError('F90.4: deliberate per-sample error…')` ✓
- Single-log `…_A` → Samples tab: `F90.4-errored` row shows red `bi-exclamation-circle-fill` icon + RuntimeError text in Status column ✓
- Both `…-A` and `…-B` `.eval` files regenerated in `logs/90-cross/` ✓

## Before/after screenshots
- before: artifacts/accuracy/F90.4-before.png (multi-log grid), F90.4-before-root.png (directory listing showing top-right buttons), F90.4-before-banner.png, F90.4-before-singlelog.png
- after: artifacts/accuracy/F90.4-after.png (multi-log grid via top-right Samples button), F90.4-after-banner.png

# F90.7 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel, expanded, inside SOLVER span — no chain() problem)
**Inaccuracies found:** 4

## Location-name verification (per task brief)

| Location | Description said | Screen actually shows | Result |
|---|---|---|---|
| Log-list Model column | "log-list grid → **Model** column" | column header `Model`, cell `none/none` | ✓ accurate |
| Models tab card | "ModelCard — `main → none/none`" | card titled **`EVAL`** → `MODEL: none/none` | ✗ wrong label — fixed |
| Title bar | "model name is blank/suppressed" | subtitle reads `WORKER:mockllm/model` (sentinel suppressed, role shown in its place) | ✗ "blank" misleading — fixed |

## Inaccuracies fixed

1. **Title bar — "blank/suppressed"** → **"the sentinel is suppressed and the subtitle shows the role `WORKER:mockllm/model` instead"**. The title bar is not blank; `PrimaryBar` falls through to rendering the model_role. The per-finding verdict already flagged this imprecision.
2. **Models tab — "`main → none/none`"** → **"the **EVAL** card → `MODEL: none/none`"**. The card header is the literal string `EVAL` (not `main`). DOM: `#task-model-card-body` inner_text begins `EVAL\nMODEL\nnone/none`.
3. **extra — "leaking `none/none` main row"** → **"a real **WORKER** card alongside the leaking **EVAL** card"**. Same label correction; also clarifies the second card is titled `WORKER`.
4. **Multi-log Samples grid — vague navigation** → **"from the log-list, click the **Samples** toggle (top-right) → Model column"**. The original gave no path to reach this view. Verified: clicking the `Samples` segment button at the top-right of the log-list shows a grid whose `Model` column for the F90.7 row reads `none/none`.

Also tightened `observed` to mirror the title-bar correction and use the `EVAL` label, and added a breadcrumb hint ("click `90-cross` in the breadcrumb") for reaching the log-list from inside the log.

## Before/after screenshots

- before: `artifacts/accuracy/F90.7-before.png`
- after: `artifacts/accuracy/F90.7-after.png`
- supporting probes: `artifacts/accuracy/F90.7-probe-titlebar.png`, `F90.7-probe-models.png`, `F90.7-probe-loglist.png`, `F90.7-probe-samplesgrid.png`

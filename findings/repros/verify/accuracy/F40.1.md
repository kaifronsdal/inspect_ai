# F40.1 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel, expanded, first event)
**Inaccuracies found:** 2

## Inaccuracies fixed

1. "expand the **Score** event" → "in the **Score** event" — the Score event panel has **no collapse chevron** (`bi-chevron-right`/`-down` count = 0); it is always rendered open, so there is nothing to "expand".
2. "its Metadata section" → "click the **Metadata** sub-tab pill" — the Score event renders two pill sub-tabs (`EXPLANATION` | `METADATA`), and the metadata tree only appears after **clicking** the Metadata pill (it is not a passively-visible section; the Explanation pill is selected by default).

Also added "→ scroll to the **METADATA** card" to the Metadata-tab path since that tab leads with USAGE and TIME cards before the RecordTree.

## Verified accurate (unchanged)

- **Metadata** tab name — present, labelled `METADATA`.
- Key names — `deep_tree` › `L1`…`L6` › `L7_leaf` and `deep_tree` › `wide_branch` › `child_00`…`child_11` all rendered exactly as named in `observed`.
- "fully expanded on first render … No node is collapsed by default" — caret census in both locations: 8 × `bi-caret-down-fill` (open), 0 × `bi-caret-right-fill` (closed).
- "wide_branch (12 children)" — `child_00`…`child_11` all present.
- `expected` technical claim (`useCollapsibleIds` returns `{}` so `if (collapsedIds) return;` short-circuits) — matches source per `per-finding/F40.1.md`.

## Before/after screenshots

- before: artifacts/accuracy/F40.1-before.png (transcript), artifacts/accuracy/F40.1-before-metadata.png, artifacts/accuracy/F40.1-before-score.png
- after: artifacts/accuracy/F40.1-after.png (transcript + Score→Metadata pill clicked), artifacts/accuracy/F40.1-after-metadata.png

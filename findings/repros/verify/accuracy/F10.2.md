# F10.2 — Description Accuracy

**Verdict (unchanged):** REPRO_BROKEN (scout-only — source-level bug confirmed; unreachable in inspect viewer)
**Banner visible at Default:** yes (INFO: BUG-REPRO panel expanded inside SOLVER span)
**Inaccuracies found:** 4

## Inaccuracies fixed

1. **Title** — "Standalone tool message never shows `error` and filters content to text+image only" → "**⚠️ SCOUT-ONLY — does not reproduce in inspect viewer.** In inspect, the error IS shown (see below). The buggy code at `ChatMessage.tsx:116-129` only runs when `collapseToolMessages=false`, which only `apps/scout` sets." — Repro is being deleted; banner must lead with scout-only warning so nobody is misled into hunting for a bug that isn't visible here.
2. **Where to look** — "If the viewer exposes a `collapseToolMessages` toggle, turn it **OFF**" → "row 2 — the `rich_tool(q: \"x\")` result. There is **no** `collapseToolMessages` toggle in inspect" — The toggle does not exist; the old instruction was impossible to follow.
3. **Observed** — "Only the text part renders. The `ContentAudio` and `ContentData` blocks are gone … the `ToolCallError` is nowhere to be seen — the failed call looks like a successful one" → "the grey output box under `rich_tool(q: \"x\")` shows `F10.2_ERROR_FIELD_timeout — exceeded 30s …` — the error **is** visible (collapsed path substitutes `error.message` for the content list)" — Screenshot shows the exact opposite of the old claim: error sentinel IS rendered; text/audio/data parts are *replaced* by it, not selectively dropped.
4. **Expected** — "Audio player, ContentData record-tree, AND a distinctly-styled error block all render" → "n/a in inspect. In `apps/scout` … the standalone branch should render `message.error` and not filter content … but this .eval cannot demonstrate that" — Old expected described behaviour that cannot be exercised from `inspect view`.

Also added to `extra`: explicit note that **this repro will be deleted** since the .eval cannot reach the cited code path.

**Note on prescribed lead text:** The task instruction said to write "the error and ContentData ARE shown". On-screen evidence (row 2 + per-finding/F10.2.md DOM dump) shows ContentData is **not** rendered — `error.message` *replaces* the entire content list on the collapsed path. Corrected to "the error IS shown" to keep the banner factually accurate per protocol.

## Before/after screenshots
- before: artifacts/accuracy/F10.2-before.png (transcript), artifacts/accuracy/F10.2-messages-before.png (messages)
- after: artifacts/accuracy/F10.2-after.png (transcript), artifacts/accuracy/F10.2-messages-after.png (messages)

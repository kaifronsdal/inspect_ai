# F30.4 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel expanded near top of Transcript; inside `SOLVER: RAISE_AFTER_GENERATE` span but not collapsed)
**Inaccuracies found:** 3

## Inaccuracies fixed

1. **Claimed fields vs. what success logs actually show.** Old `observed`: "Dataset, Scorer, Config (`distinctive_arg=F30.4_TASK_ARG`), and Duration are all missing". But every success log in this batch (F30.1, F31.1–F31.6) shows only `DATASET / SCORER(S) / DURATION` — **none has a `CONFIG` cell** (no task args). A user following "compare against F30.1" would not see Config there either and conclude the description is wrong. → New `observed` lists only the three fields the user can actually verify by comparison; the Config cell is moved to `extra` with an explicit note that the comparison logs lack it because they have no task args.

2. **"dataset name"** in `expected` — the Dataset cell renders `formatDataset(sampleCount, epochs, name)`, which for this unnamed inline dataset would show `1 sample`, not a name. → changed to "sample count".

3. **Vague comparison hint.** Old `where_to_look`: "F30.1 … DOES show the secondary bar" without saying what it looks like. → now states the exact text the user will see on F30.1: "second row reading `DATASET … SCORERS … DURATION …`" (verified: F30.1 navbar inner_text contains all three labels, plural SCORERS).

Also tightened `observed` to name the `TASK FAILED` badge (the visible landmark where the header stops) and to point at the **log-level** JSON tab specifically with the exact keys to look for (`task_args.distinctive_arg`, `stats.started_at`/`completed_at`) — all verified present.

## Re-verification

- Error-log navbar: single row ending in `TASK FAILED (1 SAMPLE)`; no `DATASET`/`SCORER`/`DURATION` text. ✓ matches new `observed`.
- F30.1 navbar: contains `DATASET` / `SCORERS` / `DURATION`, no `CONFIG`. ✓ matches new `where_to_look` + `extra`.
- Log-level JSON tab: contains `distinctive_arg`, `started_at`, `completed_at`. ✓ matches new `observed`.

## Before/after screenshots
- before: artifacts/accuracy/F30.4-before.png (transcript), artifacts/accuracy/F30.4-before-loglevel.png (header)
- after: artifacts/accuracy/F30.4-after.png (transcript), artifacts/accuracy/F30.4-after-loglevel.png (header)
- comparison: artifacts/accuracy/F30.4-compare-F30.1.png (F30.1 success navbar showing the 3 fields)

# F04.5 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel, expanded, top of transcript)
**Inaccuracies found:** 3

## Inaccuracies fixed

1. **where_to_look** — `"check the title, Summary, All, and Timing tabs"` → `"check the title bar and the Summary / All sub-tabs (the All tab has USAGE, TIMING, and MESSAGES sections)"`
   - There is **no Timing sub-tab**. The Model Call panel has exactly two sub-tabs: `SUMMARY` and `ALL`. TIMING is a *section* inside the All sub-tab, not a tab of its own.

2. **where_to_look** — `"expand Model Call: mockllm/model"` → `"Model Call: mockllm/model panel"`
   - The panel has no expand/collapse chevron (chevron-right/down count = 0); it is rendered open and is not expandable. "Expand" is a misleading verb here.

3. **observed** — `"The event title, body, and Timing panel are all silent"` → `"The title bar, Summary tab, and the USAGE/TIMING sections under the All tab are all silent — no retries row, no cache row"`
   - "Timing panel" suggested a separate panel/tab; rephrased to point at the actual TIMING section under the All sub-tab and to enumerate exactly what was checked.

## Verified accurate (unchanged)

- **extra / JSON tab reference:** sample-level **JSON** tab exists; raw event contains `"event": "model"`, `"retries": 3`, and `"cache": "read"` (grep-verified). Slightly tightened wording to `search for "event": "model"` so the user knows what to Ctrl-F for.
- **expected:** unchanged — describes desired behaviour, no UI claims.

## Re-verification (after regen)

- Panel sub-tabs: `['SUMMARY', 'ALL']` ✓
- All-tab sections present: USAGE ✓ / TIMING ✓ / MESSAGES ✓
- All-tab contains RETRIES row: no ✓ (bug) / CACHE row: no ✓ (bug)
- JSON tab contains `"retries": 3` ✓ and `"cache": "read"` ✓

## Before/after screenshots

- before: `artifacts/accuracy/F04.5-before.png`
- after: `artifacts/accuracy/F04.5-after.png`

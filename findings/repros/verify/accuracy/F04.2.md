# F04.2 — Description Accuracy

**Verdict (unchanged):** CONFIRMED (downgrade HIGH → MEDIUM)
**Banner visible at Default:** yes (INFO: BUG-REPRO panel, expanded, top-level — no fix needed)
**Inaccuracies found:** 5

## Inaccuracies fixed

1. **"Summary / All / Timing tabs"** → **"SUMMARY / ALL / API pills"** — there is no "Timing" tab; the third pill is API. Timing is a section inside the ALL view, not a sub-tab. (This was the inaccuracy already flagged in `per-finding/F04.2.md`.)
2. **Sub-tab casing `Summary / All / Tools`** → **`SUMMARY / ALL / TOOLS`** — pills render uppercase via CSS `text-transform`; description now matches what the user sees on screen (DOM `inner_text`: `['SUMMARY', 'ALL', 'API']`).
3. **"expand Model Call: mockllm/model"** → **"MODEL CALL: MOCKLLM/MODEL panel"** — the panel is auto-expanded under the Default filter (no chevron, content already visible), so "expand" is a misleading instruction. Also corrected casing to match the rendered panel title.
4. **"tab strip"** → **"pill nav … (top-right)"** — the control is a Bootstrap `nav-pills` group in the panel header's right side, not a tab strip; added position hint so the user looks in the right place.
5. **Contrast filename `F01.2_tool_choice_literal_dollar.eval`** → **`F01.2-tool-choice-literal-dollar`** — actual log filenames use hyphens, not underscores; user would not find it as written.

## Additions

- Added to `observed`: "The tool definition and `tool_choice` are visible only in the raw API tab JSON" — clarifies why the missing pill matters even though data isn't lost.
- Added source-file reference `ModelEventView.tsx:198` to the root-cause line.
- Added **severity-downgrade note** per verdict: "severity downgraded HIGH → MEDIUM — the tool definition is still recoverable from the API tab's raw request JSON; impact is informational. Duplicate of F01.4."

## Re-verification

- Banner visible at top under Default filter: ✓
- `where_to_look` path works as written: Transcript tab ✓ → MODEL CALL: MOCKLLM/MODEL panel present ✓ → pill nav top-right ✓
- `observed` matches screen: pill nav = `['SUMMARY', 'ALL', 'API']`, no TOOLS ✓
- Contrast log `F01.2-tool-choice-literal-dollar` exists in `logs/01-events/` ✓

## Before/after screenshots

- before: artifacts/accuracy/F04.2-before.png
- after: artifacts/accuracy/F04.2-after.png

# F20.6 — Description Accuracy

**Verdict (unchanged):** FALSE_POSITIVE
**Banner visible at Default:** yes (INFO: BUG-REPRO panel, expanded, 2nd in transcript)
**Inaccuracies found:** 5

## Inaccuracies fixed

1. **Missing FALSE_POSITIVE marker** → title now leads with `**✅ FALSE POSITIVE — NOT A BUG.**` so a human opening the log isn't told to look for a bug that doesn't exist.
2. "the **Transcript** tab's *Sample Limit* event" → "the **TOKEN LIMIT EXCEEDED** event panel at the bottom" — the panel's actual on-screen title is `TOKEN LIMIT EXCEEDED`, not "Sample Limit".
3. "Look at: the **Limit** column in the sample list, the sample **header row**, … the **Metadata** tab" → dropped. The sample-dialog header renders only `ID | INPUT | TARGET` (no Limit field), so directing the user there is a dead end. Replaced with the single accurate location.
4. "The number **`12345`** appears nowhere outside the raw JSON tab — Ctrl-F for `12345` to confirm" → "The panel body reads **`Token limit exceeded. value: 20,000; limit: 12,345`** — the numeric threshold is displayed." The original claim was factually wrong; the value *is* shown. Added a note that the rendered text uses a thousands separator (`12,345`), which is why the original Ctrl-F instruction failed.
5. "Expected: at least one display should read `token (12345)`" → "Exactly this — the limit value is surfaced in the transcript, so the original claim … is incorrect." Expected now affirms the observed (correct) behaviour rather than describing a non-existent gap.

`extra` rewritten to explain why the narrower `SampleSummaryView.tsx:76` source-level observation is unobservable here (no Limit field rendered in the header) and to recommend deletion.

## Re-verification

- New log: `2026-04-24T22-05-56-00-00_F20.6-limit-value-never-shown_NdwdLkHBiywNEd7sV29RQh.eval`
- `where_to_look` path checked: Transcript tab (default filter) → TOKEN LIMIT EXCEEDED panel → body text `Token limit exceeded. value: 20,000; limit: 12,345` — present and visible.
- `observed` matches DOM `inner_text` exactly.

## Before/after screenshots

- before: artifacts/accuracy/F20.6-before.png
- after: artifacts/accuracy/F20.6-after.png

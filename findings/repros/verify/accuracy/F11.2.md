# F11.2 — Description Accuracy

**Verdict (unchanged):** CONFIRMED
**Banner visible at Default:** yes (INFO: BUG-REPRO panel, expanded, directly under SOLVER span)
**Inaccuracies found:** 4

## Which tab/panel actually shows the error

**Messages tab only** — row 2, the grey `tool-output` box under the `restricted_op` header. The Transcript tab does **not** show it: this repro injects `ChatMessageTool` directly without executing a tool, so no `ToolEvent` is emitted and the Transcript contains only `SOLVER: INJECT_TYPED_ERROR` + `INFO: BUG-REPRO`.

## Inaccuracies fixed

1. **`where_to_look`**: "and **Transcript tab** if a Tool event is present" → "**Messages tab** → row 2 → the grey output box under `restricted_op`. (The Transcript tab has no Tool event in this repro — Messages is the only place to look.)" — old text sent users to a tab where the bug isn't observable; the hedge "if … present" is confusing when it never is.
2. **`observed`**: "Ctrl-F for `F11.2_TYPE_FIELD_VALUE_permission` → 0 matches" → dropped entirely. This was **factually false**: the sentinel string appeared on-screen multiple times (in the banner and inside `error.message` itself), so Ctrl-F found ≥3 matches. Replaced with a concrete on-screen description: "grey `tool-output` box renders `error.message` verbatim … no `permission:` prefix, no error-type badge, no visual cue that this is an error".
3. **`extra`**: "Sentinel for absence-proof: `F11.2_TYPE_FIELD_VALUE_permission`" → dropped. A sentinel can't be placed in `error.type` (it's a `Literal` enum), so the absence-proof was meaningless. Replaced with a working verification path: JSON tab → search `"error"` → shows `"type": "permission"`.
4. **`error.message` body**: the old message was self-contradictory — it said "grep for `F11.2_TYPE_FIELD_VALUE_permission`: you will NOT find it" while *containing* that exact string. Replaced with a clean realistic message (`Access denied to /etc/shadow. [This is error.message — note error.type is NOT shown anywhere alongside it.]`). After this change the word `permission` does not appear anywhere in the rendered tool-call DOM (verified via outerHTML scrub), making the bug unambiguous.

## Re-verification

- New log: `2026-04-24T21-48-29-00-00_F11.2-tool-error-type-dropped_Zcse3VN6qv2UQ7tJMioYhD.eval`
- Messages tab row 2 toolCallView text = `restricted_op\nAccess denied to /etc/shadow. […]` — zero occurrences of `permission` in element HTML.
- JSON tab contains `"type": "permission"` ✓
- Every navigation step in the new `where_to_look` works as written.

## Before/after screenshots

- before: artifacts/accuracy/F11.2-before.png (transcript), artifacts/accuracy/F11.2-before-messages.png
- after: artifacts/accuracy/F11.2-after.png (transcript), artifacts/accuracy/F11.2-after-messages.png
